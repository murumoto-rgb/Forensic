# Node / pnpm
node_modules/
*.log
.pnpm-store/
dist/
build/
.next/
.vite/
coverage/
*.tsbuildinfo

# Puppeteer Chromium cache (Build #5.74.1). On Render this lives at
# /opt/render/project/src/.puppeteer-cache so the build-time install
# survives into the runtime container; locally it lands inside a
# dev's repo too if they ever launch the worker.
.puppeteer-cache/

# OS / editor
.DS_Store
.vscode/
.idea/

# Env
.env
.env.local
.env.*.local

# Xcode
ios/build/
ios/*.xcodeproj/xcuserdata/
ios/*.xcworkspace/xcuserdata/
ios/DerivedData/

# xcodegen-generated artifacts (source of truth is ios/project.yml).
# Running `xcodegen generate` rewrites these from the inline
# `info.properties` / `entitlements.properties` blocks.
ios/SitePhoto/Info.plist
ios/SitePhoto/*.entitlements
ios/SitePhoto.xcodeproj/

# Auto-generated by ios/scripts/gen-build-info.sh — regenerated on
# every `regen-project.sh` run.
ios/SitePhoto/Generated/
