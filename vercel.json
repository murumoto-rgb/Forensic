{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm --filter @forensic/web build",
  "installCommand": "corepack enable && pnpm install --frozen-lockfile",
  "outputDirectory": "web/dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
