## Build 5.43.1

* **Date:** 2026-06-04
* **PR:** [#73](https://github.com/murumoto-rgb/Forensic/pull/73)
* **Branch:** claude/web-validator-repair
* **Merge SHA:** 2c25f34
* **Summary:** Sprint B of the AI-tagging completion plan — port iOS's `AIResponseValidator` to TypeScript and wire it into both web re-tagging surfaces with a one-shot repair retry on vocabulary rejections. New `packages/shared/src/aiResponseValidator.ts` mirrors iOS's `AIResponseValidator.validate(...)` checks: primary tag count cap (≤2), unknown primary, missing/empty `secondary_tags_by_primary` entry, secondary not under chosen primary (case-insensitive against vocabulary), constrained-enum validation for `scalePresent` (`Yes`/`Relative`/`No` + legacy `Partial`) and `recommendedUse` (`Body figure`/`Appendix only`/`Context/locator`/`Re-shoot recommended` + spelling variants), causation-phrase scan on `summaryObservation` (`caused by` / `due to` / `because of`), tag-confidence out-of-range + missing-secondary-entry checks. Also `resolveValidationVocabulary({library, selection, extras})` — same merge discipline iOS uses, extras union into the library's primaries/secondaries sets. New `repairUserPromptText(errors)` composes the same bulleted follow-up message iOS sends. New `web/src/lib/tagPhotoFlow.ts:tagPhotoWithValidation(...)` is the shared helper both `ReTagWithAIControl` and `useBatchRetag` now use — single call → parse → (optional) validate → on validation errors (and not parse-failed) do one-shot repair retry with prior raw response + validator bullets embedded in the second call's `userText` (proxy endpoint is single-turn so we wrap rather than send true multi-turn). Repair-retry parse failure or network failure falls back to first result so engineer at least sees what model produced — mirrors iOS's `tag(...)` `catch` discipline. UI feedback: single-photo Re-tag status banner has four variants now (clean / repaired-clean / repaired-with-issues / unrepaired-with-issues) so the engineer can tell whether the model needed a second pass. Batch runner takes the same vocabulary and runs the same repair flow per photo. Snapshot tests at `tests/aiResponseValidator.test.ts` (20 cases, 50 parity assertions total) lock in every check arm + vocabulary resolution + extras merge + repair-prompt format. Web-only PR. No iOS, server, supabase migration, or `manifestSchemaVersion` change.

---
