## Build 6.29.1

* **Date:** 2026-06-10
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/review-code-visibility-izhPs
* **Merge SHA:** TBD
* **Summary:** **Photos tab list ⇄ grid toggle (Wave 4).** A new layout button in the photos header (grid icon ⇄ list icon, next to Select) switches the photo list between the classic rows and a **3-up adaptive thumbnail grid**. The choice persists via `@AppStorage("sitephoto.photoLayout")` (stored on the main struct — extensions can't hold stored properties; the `PhotoLayout` enum + computed accessor live in the Photos extension). **Grid cells:** `CachedThumbnail` (the #6.4.1 async cache — the grid stays smooth for free) at 4:3, sequence-number capsule bottom-left, favorite star top-right, and full **selection-mode support**: tap toggles membership with a checkmark + accent border, and the batch actions row works exactly as in list mode. Tap outside selection mode opens the photo editor (same action as tapping a row's thumbnail). **Deliberate scope:** swipe actions, drag-out, and the per-row action buttons remain list-mode features — the grid is for visual browsing and fast multi-select, matching the Photos-app convention. The layout toggle is available on frozen projects (it's a view preference, not an edit); the grid honors all the existing filters since it renders `visiblePhotos`. The plan viewer's "open in list" scroll targeting keeps working (`.id(photo.id)` on cells). Backlog row updated. iOS-only; no DB / shared / server / web change.

---
