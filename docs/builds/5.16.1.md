## Build 5.16.1

* **Date:** 2026-06-01
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/phase3-retry-groups-resetview
* **Merge SHA:** TBD
* **Summary:** Three web fixes on the floor-plan canvas. (1) Pin-save retry extended from 1 to 3 attempts with exponential backoff (2s → 5s → 10s = up to ~17 sec) so a Render free-tier cold-start (typically ~20-30 sec) succeeds without surfacing an error. (2) Reset view button is now always visible (amber when panned/zoomed, neutral when at the auto-fit baseline) so the user can easily recover if they've zoomed into an empty area of the plan. (3) Photo group pins now render only the primary; a small "+N" amber badge on the upper-right of the pin indicates how many reshoots stack underneath. Footer text spells out the breakdown when reshoots are suppressed. Web-only; no iOS, server, or schema change.

---
