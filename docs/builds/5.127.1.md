## Build 5.127.1

* **Date:** 2026-06-09
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/ios-workspace-shell
* **Merge SHA:** TBD
* **Summary:** **iOS IA refactor — Phase 1/7.** Introduces the tabbed project workspace shell that PRs 2-6 will progressively fill in. New files: `ios/SitePhoto/Views/Workspace/ProjectTab.swift` (an enum carrying label + SF Symbol for each of the five tabs — Photos / Plan / AI / Export / More), `ProjectWorkspaceModel.swift` (an `@Observable @MainActor` class that for now only owns `selectedTab` but is the seam for cross-tab state that lands in subsequent PRs: search text, filter bundle, selection mode, scrollToPhotoID, batchTagInFlight), `ProjectWorkspaceView.swift` (the shell — a `TabView(selection:)` whose Photos tab embeds today's full `ProjectDetailView(projectID:)` so every existing per-project feature remains accessible while the other four tabs render a "coming soon" placeholder explaining which PR will land their content). Modified `ContentView.swift:97-99` — the only behavioural change in PR 1 — swapping `.navigationDestination(for: Project.self)` from `ProjectDetailView` to `ProjectWorkspaceView`. The shell allocates its model in a `.task(id: projectID)` (standard SwiftUI workaround for "environment isn't readable in `init`"); the model is keyed on `projectID` so navigating between two projects rebuilds it. **No content was rewritten** — the embedded ProjectDetailView keeps its `.searchable`, toolbar, FAB stack, and every sheet exactly as today; subsequent PRs will lift sections out one at a time. Out of scope for PR 1: the shell-level FAB overlay, the shell-level auto-save indicator, the `LocateSheet`/`FloorPlanAssignmentSheet` shell-level presentation, and the actual tab-content carve-outs (those are PRs 2-6). No `MANIFEST_SCHEMA_VERSION` change; no shared/server/web touched; parity contract test green (68/68).

---
