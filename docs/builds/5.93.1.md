## Build 5.93.1

* **Date:** 2026-06-08
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/web-permanent-delete
* **Merge SHA:** TBD
* **Summary:** Permanent project deletion on web — closes the deferred half of PR #1 (Build #5.84.1). New server `DELETE /v1/projects/:id` endpoint reaps the project's R2 blobs, drops every `files` row for the project, and drops the `projects` row itself. Refuses to hard-delete an active project (returns 409 `precondition_failed` if `isDeleted !== true`) — mirrors iOS's two-step swipe-to-trash-then-swipe-to-purge flow. New `deleteObjects(keys[])` helper in `server/src/r2.ts` chunks at 1000 (the S3/R2 DeleteObjects limit) and uses `Quiet: true`; per-object errors are logged but not raised because the manifest row + file rows are deleted unconditionally — orphaned R2 blobs are an unused-storage cost, not a correctness issue. Web `api.hardDeleteProject(projectId)` is a small `fetch`-based helper that handles the 204 response shape. `TrashedProjectRow` grows a second "Delete permanently" button + a confirm dialog that quotes the project name and explains what's about to be reaped. Both Restore and Delete-permanently bind to the same `reload` callback so the trashed section refreshes after either action. Parity matrix: new "Permanently delete trashed project" row is ✅ on both platforms. No schema change in shared, no iOS change. No `manifestSchemaVersion` bump.

---
