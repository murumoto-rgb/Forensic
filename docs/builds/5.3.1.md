## Build 5.3.1

* **Date:** 2026-05-30
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/builds-one-file-per-build
* **Merge SHA:** TBD
* **Summary:** Restructure build registry from one shared `docs/builds.md` to one file per build under `docs/builds/`. Eliminates the merge-conflict cascade that hit Builds #4.1.1–#4.5.1 (every PR was inserting at the top of the same file). Now each PR adds its own file; parallel PRs no longer collide. `ios/scripts/gen-build-info.sh` and `web/vite.config.ts` updated to read the latest version-sorted filename. Pairs with the "selective merging" policy from #32 — more concurrent open PRs are now collision-free.

---
