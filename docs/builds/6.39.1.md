## Build 6.39.1

* **Date:** 2026-08-31
* **Branch:** grok/wave-1-exhibit-accuracy
* **Merge SHA:** TBD
* **Summary:** Wave 1 exhibit accuracy, no schema bump. GPS stamps and report covers print hemisphere letters with absolute values (Texas west longitude is `W`, not a negative easting). Web can re-calibrate an existing floor plan (pins stay on image pixels; feet re-derive). Server/web PDF plan pins honor the on-screen color mode. Shared `formatLatLon` / `pinColorFor` are the single source for those strings and fills.
* **Validation:** Shared GPS and pin-color unit tests; web recalibration unit tests; server PDF option defaults; typecheck. Physical iPhone stamp on a Texas capture remains a device check.
* **Delivery:** Feature branch. Does not apply migrations 0016–0019 and does not replace the v4 cutover.

---
