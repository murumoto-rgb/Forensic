## Build 6.9.1

* **Date:** 2026-06-10
* **PR:** [#178](https://github.com/murumoto-rgb/Forensic/pull/178)
* **Branch:** claude/review-code-visibility-izhPs
* **Merge SHA:** ac387f1
* **Summary:** **AI-run guardrail + batch progress thumbnail (Wave 3, PR 3 — closes the iOS review's approved scope).** (1) **Run buttons disable when AI Tags isn't configured.** With an empty/cleared tag selection, "Auto-tag untagged" and "Auto-tag every photo" used to be tappable and then fail at runtime with the "Pick at least one investigation context first" toast. Both now disable up front, with an orange caption in the Run section explaining the one required step ("pick at least one investigation context under Vocabulary above") and clarifying that Job-Specific Vocabulary and AI Notes are **optional** — the progressive-disclosure complaint from the review, addressed without hiding any rows ("Clear AI tagging" stays enabled; it doesn't need a selection). New projects are unaffected (they're created with the full default selection); this matters for older projects or after clearing the selection. (2) **Batch progress shows the in-flight photo.** The full-screen tagging overlay showed "Tagging N of M · Photo #seq"; it now also renders the photo's **thumbnail** (via #6.4.1's `CachedThumbnail`, 64×48) next to the sequence number — a stuck batch becomes diagnosable at a glance ("it's sitting on the dark stairwell shot"). The thumbnail resolves by sequence number once per progress tick. **Scope note:** the review's other progressive-disclosure item (label Vocabulary/Notes rows optional) was found already shipped — both rows' empty-state subtitles have said "Optional …" since #5.130.1. No DB / shared / server / web change; parity contract unaffected.

---
