## Build 5.9.1

* **Date:** 2026-05-31
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/phase3-ios-plan-upload
* **Merge SHA:** TBD
* **Summary:** Phase 3 PR-B — iOS uploads floor plan image binaries to R2. PhotoSyncer's `syncProject(_:)` now iterates `project.floorPlans` after photos, and a new `uploadPlanIfNeeded(plan:in:)` helper does the same presigned-URL → R2 PUT → commit flow PhotoSyncer already does for photos. Object key uses the plan UUID in the photo_id slot (server's `files` table reuses the column for plan IDs when `kind=plan`). After install + sync, the floor plan canvas on web fills in the background image and clears the amber "pending upload" banner. No schema change; no server change.

---
