## Build 6.26.1

* **Date:** 2026-06-10
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/review-code-visibility-izhPs
* **Merge SHA:** TBD
* **Summary:** **Web plan color modes (Wave 4 — closes the #5.103.1 "Round 3" deferral).** Web floor-plan pins were hardcoded blue; iOS has had status/bucket/primaryTag coloring via `PlanColorMode` since #5.69.1-era work. The #5.103.1 deferral called the pin renderer "invasive" — on inspection it needed one prop: `PhotoPin.fillColor` (default = the legacy blue; `highlighted` amber still wins so the selected pin stays findable in any mode). **New `web/src/lib/planMarkerColors.ts`** mirrors iOS's `PlanMarkerColors`: `status` → the platform default color, `bucket` → the bucket's `colorHex` (neutral grey for unbucketed/deleted), `primaryTag` → a deterministic hue from the photo's first primary label. **Deliberate divergence, documented:** iOS spaces primary-tag hues by controlled-vocabulary rank; web has no bundled vocabulary module, so it hashes the label to a hue (golden-ratio stepping) — same visual grouping, different palette. **Mode picker** ("Color: Default / Bucket / Tag") sits on the plan toolbar next to the pin-size control, and the setting is **cross-device synced** through the #6.13.1 `useUserPrefs` pipeline (`user_prefs.planColorMode`, zod-validated enum, server null-coerce for old rows) using the same storage key + rawValues as iOS's UserDefaults entry. `FloorPlanCanvas` threads an optional `pinFillFor` resolver to both PhotoPin call sites (singleton + cluster lead). **Known remaining gap, noted in the matrix:** the web PDF export still renders the default pin color regardless of mode (iOS PDFs honor the mode) — pin *size* parity shipped in #6.20.1; color-mode parity in the PDF is a follow-on. Typecheck clean across web + shared + server; 68/68 shared tests (UserPrefs in the parity ignore set).

---
