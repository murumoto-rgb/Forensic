## Build 6.23.1

* **Date:** 2026-06-10
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/review-code-visibility-izhPs
* **Merge SHA:** TBD
* **Summary:** **ProjectDetailView decomposition, part 1 of 2 (Wave 4).** The 3,442-line file carried seven file-scope support structs below the main view. They move out verbatim: **`PhotoRow.swift` (243)** — the photo list row (thumbnail, badges, action buttons, swipe content); **`BatchTagSupport.swift` (187)** — `BatchTagFailureReport`, `BatchTagPrompt`, `BatchTagModifiers` (the 3-alerts+overlay bundle), `BatchTagProgressOverlay`; **`ProjectDetailSupportModifiers.swift` (183)** — `PlanAndDeletionModifiers` (the 5-modifier type-check-budget bundle) and `SearchableIfPhotos`. `ProjectDetailView.swift` drops to **2,851 lines**. **The only textual change besides relocation:** the seven structs lose their file-scope `fileprivate`/`private` (becoming internal — required for cross-file reference within the module); every struct's *members* keep their original access. This is the lowest-risk slice of the decomposition by construction: the moved structs were verified to take all inputs via init parameters/bindings (no reach-back into ProjectDetailView's private state), their external dependencies (`CachedThumbnail`, `ProjectStore.BatchTagResult`/`BatchTagMode`) were already cross-file-visible, and docblocks traveled with their declarations (comment-aware splitter; one pre-existing stale comment above `PlanAndDeletionModifiers` traveled as-is). **Verification without a Swift compiler:** per-file brace balance checked programmatically (655/655, 47/47, 42/42, 50/50), main-file references to the moved types confirmed present (15 sites), zero leftover file-scope structs in the main file, and the new files are picked up by xcodegen's directory scan. **Part 2** (extension-splitting the per-tab section builders out of ProjectDetailView itself, which requires access-level changes on shared private state) follows as its own PR so each step stays independently revertable. No behavior change intended anywhere. No DB / shared / server / web change.

---
