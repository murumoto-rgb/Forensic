## Build 6.35.1

* **Date:** 2026-06-11
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/p2c-ios-readme-build-path
* **Merge SHA:** TBD
* **Summary:** **iOS README points at the working build path (closes a P2 reviewer finding).** The setup steps in `ios/README.md` told the user to `brew install xcodegen && cd ios && xcodegen`, which skips `ios/scripts/gen-build-info.sh` — the script that writes the gitignored `ios/SitePhoto/Generated/BuildInfo.swift` that `SettingsSheet.swift:383` reads for the in-app About display (git SHA / branch / build timestamp). New clones following the README literally would fail to build (missing `BuildInfo.swift`); existing clones got stale or empty SHA/build info. `CLAUDE.md` already documented the correct wrapper (`ios/scripts/regen-project.sh`); the public-facing README had drifted. **Fix:** rewrite both setup snippets to call `ios/scripts/regen-project.sh` (canonical) or the day-to-day `scripts/sync-ios.sh <branch>` one-liner (pulls + regens + builds + installs in one shot). Added a "Don't run `xcodegen` directly" callout above the first snippet explaining why the wrapper exists. The "Why XcodeGen?" section at the bottom stays — it still accurately describes the `.xcodeproj` generation strategy; only the *commands* needed updating. **Parity check:** docs-only; no code touched.
