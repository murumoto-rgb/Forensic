## Build 5.121.1

* **Date:** 2026-06-09
* **PR:** [#155](https://github.com/murumoto-rgb/Forensic/pull/155)
* **Branch:** claude/roles-migrations
* **Merge SHA:** 27e721a
* **Summary:** **Phase 3, PR 3.1** — additive migrations for users + roles + per-project assignment. **Migrations only; zero behavior change**; the server access refactor (PR 3.3) + admin API (PR 3.4) + web UI (PR 3.5) layer on top. `0014_collaboration_roles.sql` adds `profiles.is_admin boolean not null default false` (single-team org-admin flag, the natural home for the hot access-check path) and creates the `project_members (project_id, user_id, role text check in ('editor','viewer'), added_by, added_at, pk(project_id, user_id))` join table with indexes on `user_id` (for the user → projects list union) and `project_id` (for the per-project assignment editor). The owner stays an implicit Editor — `projects.owner_id` is never duplicated into the join table, keeping today's `.eq("owner_id", request.user.id)` queries working unchanged until the access refactor lands. **Defense-in-depth RLS**: `project_members` SELECT is restricted to own-membership rows (narrow on purpose to avoid a policy cycle with `projects`); a permissive policy on `projects` ORs in member access; a parallel policy on `files` widens its existing owner-scope to members and admins. Real authorization stays in the upcoming server `access.ts` helper — RLS is the secondary layer for any future publishable-key reads. `0015_seed_first_admin.sql` flips `is_admin = true` for the current sole user `mb@baykalconsulting.com` (case-insensitive match, idempotent, safely no-ops if the user hasn't signed in yet). **Run order**: 0014 then 0015. Both are safe to re-run. No DB downtime; no existing query semantics change because additive columns and tables are invisible to old code. No manifest-model change; parity untouched.

---
