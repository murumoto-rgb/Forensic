## Build 6.13.1

* **Date:** 2026-06-10
* **PR:** [#182](https://github.com/murumoto-rgb/Forensic/pull/182)
* **Branch:** claude/review-code-visibility-izhPs
* **Merge SHA:** 02756f2
* **Summary:** **Web review Wave 1, PR 3: floor-plan pin-size cross-device sync.** Build #5.103.1 shipped per-browser localStorage persistence for the FloorPlanTab's pin-size (`+ / −`) buttons and explicitly deferred cross-device sync via `useUserPrefs` as a follow-on. Closed now. **New field:** `UserPrefs.planBubbleScale: number | null` added to the shared TS interface (`packages/shared/src/api.ts`) and the zod schema (`packages/shared/src/validation.ts`, range 0.5–4 to match the FloorPlanTab clamps). No DB migration — `user_prefs.prefs` is a `jsonb` blob (migration 0010) so new pref fields ride for free, exactly as documented at the table. **Hook plumbing** in `web/src/lib/useUserPrefs.ts`: new `readBubbleScale` reader (same shape as `readThreshold`); `bubbleScale` + `setBubbleScale` exposed on the hook return; hydrate-on-mount writes the server value into localStorage only when the key is unset (preserving the "local choice wins" invariant the other three prefs already follow); `scheduleSync` PUTs the field through the existing 1-second debounce; the cross-tab `storage` event listener watches the new key. **FloorPlanTab** drops its bespoke `useState<number>` + ad-hoc localStorage writer in favor of the hook — the read-and-write surface stays exactly the same (same key, same default 1.5×, same min/max/step), but a pin-size change on the laptop now follows the user to the iPad. Old in-flight sessions read whatever localStorage already has; the first server PUT after this build lands it into `user_prefs.prefs.planBubbleScale`. **Parity matrix:** new "Floor-plan pin size (per-user)" row added. iOS column is `n/a` for the on-screen viewer (its setting is separate; the iOS PDF exporter does read the same key, which is what synced #5.103.1 deferred). **Parity contract test:** `UserPrefs` is in the test's `APP_CONFIG_STRUCTS` ignore set — adding a field to it is safe without iOS changes (web-only wire type by design). Server + web typecheck + 68/68 shared tests clean.

---
