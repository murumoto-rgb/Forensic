## Build 5.45.1

* **Date:** 2026-06-04
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/server-audit-log
* **Merge SHA:** TBD
* **Summary:** Sprint E1 of the AI-tagging completion plan — `audit_log` table + cost-tracking writes from the AI proxy. New migration `0005_audit_log.sql` adds an append-only `audit_log(id bigserial PK, event_type text, user_id uuid → auth.users, project_id uuid → projects, photo_id uuid, model text, input_tokens int, output_tokens int, cache_read_tokens int, cache_creation_tokens int, duration_ms int, cost_estimate_hundred_thousandths_of_cents bigint, detail jsonb, created_at timestamptz)`. RLS lets each user SELECT their own rows; team-wide aggregation queries go through the server's secret key. Per-event indexes on `created_at`, `user_id`, `project_id`, `event_type`. Cost stored as integer hundred-thousandths of a cent (units of 1e-5 cents) so a single low-cost call keeps non-zero precision; divide by 10,000,000 for USD. New `server/src/audit.ts` carries `estimateCostHundredThousandthsOfCents(...)` (per-model rate table: Sonnet 4.6 $3/$15 per 1M, Haiku 4.5 $1/$5 per 1M; cache reads 0.1×, cache writes 1.25×) + `recordAITagAudit(...)` (best-effort insert; logs but never throws so a database hiccup doesn't fail the proxy call). `aiTag.ts` writes one `ai_tag_photo` row per successful Anthropic call carrying user/project/photo IDs + token usage + duration + cost estimate + raw-response length in `detail`. Server-only PR; no iOS, web, shared schema, or `manifestSchemaVersion` change. Step required from operator before merge: run the migration against Supabase (same drill as previous migrations).

---
