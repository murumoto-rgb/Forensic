## Build 5.73.1

* **Date:** 2026-06-07
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/pdf-modal-parity
* **Merge SHA:** TBD
* **Summary:** **Path C #8/8 (final) — web modal rewrite.** Closes the Path C parity work the user committed to on 2026-06-07 by giving the web Export PDF modal the same option surface iOS's `PDFExportOptionsView` does. The legacy #5.64.1 fields (`photosPerPage: 1|2|4`, `photoFilter: all|favorites|byFloorPlan`, `floorPlanId: string|null`, `includeFloorPlanPages: boolean`) are retired from the web client — `DEFAULT_OPTIONS` no longer carries them, the modal no longer sets them. Server's `applyOptionDefaults` still accepts them as `Partial<PdfExportOptions>` for legacy callers (CLI / older browser tabs / etc.), so this is a clean web-side cut not a server-side break. New modal layout in `web/src/components/ExportPdfControl.tsx`: five `<fieldset>` sections in a scrollable modal — Layout (`pageSize` Letter|A4 + flexible `perPage` 1/2/3/4/6/8/9/12), Sections (Cover always-first toggle + 3 toggles for Plan / Contact sheets / Metadata table that rebuild `sectionOrder` in the iOS canonical order via the new `sectionOrderFromFlags` helper; metadataTable toggle also mirrors into `includeMetadataTable` for the server's boolean gate), Floor plans (4-option `planMode` select mirrors iOS `PlanRenderMode.displayName` exactly + "All / Specific" radio with per-plan checkboxes for `selectedFloorIds`), Per-photo annotations (5 independent checkboxes matching iOS `AnnotationOptions` — tags / caption / observation / measurement / reviewer flag — shown only when the contact-sheets section is included), Filters (`groupByBucket` + `includeTrashed`). Submit-disabled when user picks "Specific" plans without checking any — prevents silently submitting a no-floors filter. iOS-default state on first open: 6 photos per page (was 1 before), tags-only annotations, photoOnly plan mode, all-plans, plan + contactSheets sections included, metadata table off, no bucket grouping, no trashed photos. Conditional rendering: the Floor plans fieldset hides entirely when no plans exist or the user excludes the plan section; the Annotations fieldset hides when contact sheets are excluded. Web only — no server / shared / iOS / schema change. No `manifestSchemaVersion` bump. **End of Path C.** All 8 PRs landed: pipeline + Chrome fix + iOS-parity types + contact sheets + plan modes + metadata table + groupByBucket + sectionOrder + chunked rendering + parity modal.

---
