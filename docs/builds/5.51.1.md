## Build 5.51.1

* **Date:** 2026-06-06
* **PR:** [#81](https://github.com/murumoto-rgb/Forensic/pull/81)
* **Branch:** claude/backfill-diagnostics
* **Merge SHA:** ed66aef
* **Summary:** Diagnostics + manual entry point for `BinaryBackfillService` after #5.50.1's chip + toast still left "I tapped Sync now and nothing happened" cases unexplained on a real simulator session. New Settings → Sync → **"Backfill missing files"** button bypasses the full sweep so the user can isolate the backfill path from PhotoSyncer / ManifestSyncer noise. Per-project diagnostic print added to `backfillAll()`: every project now logs `[BinaryBackfill] project NAME: X photos, Y plans — missing A photo(s), B plan(s) locally` so the user can pinpoint which projects (if any) have missing binaries the service is detecting. "Nothing missing" path now posts an info toast + leaves `progress.totalFiles = 0` so the chip in the footer stays hidden when there was truly no work, but the toast confirms the sweep ran. iOS-only PR. No server / web / shared / supabase change.

---
