## Build 6.12.1

* **Date:** 2026-06-10
* **PR:** [#181](https://github.com/murumoto-rgb/Forensic/pull/181)
* **Branch:** claude/review-code-visibility-izhPs
* **Merge SHA:** c6f3e96
* **Summary:** **Web review Wave 1, PR 2: "Load template…" picker on the project AI tab + stale-copy fix.** Build #5.104.1 shipped the admin editor for team-wide AI prompt templates (`/admin/ai-prompt-templates`) and promised a project-side picker as a follow-on — never built. (iOS got the same picker via `AIPromptTemplateManagerSheet` + #6.3.1's sync.) Now: `ProjectAIConfig.tsx` fetches `app_config.aiPromptTemplates` once on mount and exposes a **"Load template…"** dropdown in the AI notes section header (only when the user can edit + the team actually has templates). The dropdown lists each template's name + a 140-char prompt preview; clicking one drops the prompt into the AI notes textarea and saves the project (same shape as `commitNotes`, including the empty→null normalisation). If existing notes are non-empty, a confirm modal appears first ("Loading X will overwrite them" / Replace / Cancel) so a template load can't silently nuke in-flight guidance. Templates load is best-effort — a fetch failure surfaces a small "templates unavailable" hint and the dropdown stays hidden; existing flows are unaffected. Empty-template-list state is also hidden (no dead UI). **Also fixed:** the stale error message in `PhotoPreviewPanel.tsx:1319` that told users to "Pick contexts on iOS (project → AI Tags) first" when re-tag failed with `PromptCompileError` — web has had its own three-level picker since #5.86.1 (the AI tab → Tag selection → Edit selection…). Message now points there. No server change (uses existing `getAIPromptTemplatesConfig`); no shared change; no iOS change. Server + web typecheck clean.

---
