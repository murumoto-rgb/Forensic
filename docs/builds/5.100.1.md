## Build 5.100.1

* **Date:** 2026-06-08
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/web-storage-status
* **Merge SHA:** TBD
* **Summary:** Round 3 PR #4 — web Storage card on the Settings page + Refresh button on the workspace header. New server endpoint `GET /v1/me/storage` aggregates per-user cloud usage: project count (active + trashed split), photo count, floor plan count, total bytes across the `files` table broken down by `kind`. New `StorageStatusCard.tsx` renders the breakdown with a Refresh button; loads on mount but doesn't auto-refresh — user driven. New shared `UserStorageStatus` + `GetUserStorageStatusResponse` types (wire-only; no zod schema needed since the route is read-only). Workspace header gains a ↻ Refresh button that calls `manifest.reload()` — the conceptual equivalent of iOS's "Sync now" (web doesn't need a full sweep because the server is always live; this is the manifest re-fetch). No iOS change. No schema change. No `manifestSchemaVersion` bump.

---
