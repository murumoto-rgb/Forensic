## Build 5.81.1

* **Date:** 2026-06-08
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/ai-config
* **Merge SHA:** TBD
* **Summary:** Path P #6/8 — project AI configuration. The AI tab gains a new `ProjectAIConfig` component above the existing batch-retag control, exposing every project-scoped AI setting iOS surfaces: AI notes (`aiInstructions`) as a textarea that commits on blur with placeholder copy explaining what to put there; job-specific vocabulary (`aiExtraVocabulary.primaries`) as a JSON textarea that parses on blur, validates the top-level shape (must be an array), and round-trips the inner records as-is since the `PrimaryTagEntry` shape isn't yet part of the manifest contract; tag-selection summary (`tagSelection`) that quotes context / primary / deselected-secondary counts plus a Clear button for wiping a stale selection (the full 3-level picker UI is a follow-on); and a "Clear AI tags" utility with two flavors — clear AI tags only or clear AI tags + analysis — each routed through a confirm dialog that previews the affected-photo count before committing. The clear logic strips every tag whose confidence is below 1.0 (the convention iOS uses for AI-attributed) so user-added tags (confidence 1.0 from the editor sheet) are preserved; the +analysis flavor additionally nulls `aiAnalysis` on each affected photo. Every write routes through `manifest.save(nextProject)` so changes use the shared optimistic-save loop. `AITab` itself shrinks to two sections: `ProjectAIConfig` on top, then the pre-existing `BatchRetagControl` below it inside a labeled section. No new API, no schema change, no shared API change, no server change, no iOS change. No `manifestSchemaVersion` bump.

---
