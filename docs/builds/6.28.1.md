## Build 6.28.1

* **Date:** 2026-06-10
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/review-code-visibility-izhPs
* **Merge SHA:** TBD
* **Summary:** **Projects-page polish (user-requested).** (1) **The Baykal logo footer at the bottom of the projects list is gone.** Removed the `safeAreaInset` bar from `SitePhotoApp`, the `FooterLogoBar` view, and the whole `atRoot` plumbing it existed for (the `@Binding` through `ContentView`, the `onChange(path.isEmpty)` reporter, and the preview's `.constant`) — ~25 lines of supporting machinery for a footer that crowded the list. The `BaykalLogo` view itself stays: the splash screen and the PDF cover still use it. (2) **"Deleted Projects" collapses by default.** On long-running installs the trash can outnumber the active list and pushed the storage footer below the fold. The section header is now a tappable "Deleted Projects · N" row with a rotating chevron (the same affordance the web list's trashed section has used since #5.84.1); rows render only when expanded, and the existing swipe actions (Restore / Delete Forever) are unchanged inside. Collapsed is the default on every launch (deliberate — trash is an occasional destination, not a working set; no persistence of the toggle). Accessibility labels on the expand/collapse states. iOS-only; no DB / shared / server / web change.

---
