## Build 5.4.1

* **Date:** 2026-05-30
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/phase3-floor-plan-viewer
* **Merge SHA:** TBD
* **Summary:** Phase 3 PR-A — read-only floor plan viewer on web. New route `/projects/:id/plan` renders the floor plan image as a react-konva canvas with photo pins (numbered, with heading arrow when available) and distress marks (colored dots for point distress; freeform stroke for `crackFloor`) overlaid in plan-pixel coordinates identical to iOS. Plan picker tabs when a project has multiple plans. Server gains `GET /v1/projects/:id/plans/:planId/image` (mirror of the photo image endpoint). Plan image 404 → "Pending upload from iPhone" placeholder; pins/distress still render on a blank background so spatial layout is visible without the underlying plan. Editing (drag pins, draw distress) lands in PR-B; iOS plan-image upload lands in PR-B; plan creation/calibration lands in PR-C.

---
