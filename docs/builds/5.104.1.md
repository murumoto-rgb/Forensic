## Build 5.104.1

* **Date:** 2026-06-08
* **PR:** [#138](https://github.com/murumoto-rgb/Forensic/pull/138)
* **Branch:** claude/web-prompt-templates-bucket-lib
* **Merge SHA:** c8659bd
* **Summary:** Round 3 PR #8 — web admin editor for saved AI prompt templates. New shared `AIPromptTemplate` + `AIPromptTemplateLibrary` types in `appConfig.ts`, matching zod schemas in `validation.ts`, new `aiPromptTemplates` app_config key plumbed into `AppConfigValueSchemaByKey`. Server picks up the key automatically. Parity test scopes the two new wire-only structs into `APP_CONFIG_STRUCTS`. New `AdminAIPromptTemplatesPage.tsx` at `/admin/ai-prompt-templates` — same shape as the other admin editors (load + draft + save with optimistic-concurrency revision token + 409 banner). Per-template card with name input + prompt textarea + delete button; "+ Add template" button appends a fresh template with a new UUID. Link added to Settings → Team-wide config section. **Bucket library** (the other PR #8 plan item) is deferred — iOS-side struct (`BucketLibraryManagerSheet`) is not yet in shared, and the project Buckets tab integration ("Load from library" / "Save to library") needs design choices the user should make. Tracked as a follow-on. **Project-side picker** for these templates (the AI tab gaining "Load template…" button) is also a follow-on — surfaces the saved templates next to `Project.aiInstructions`. iOS sync of the new key is a future iOS-only PR (same pattern as #5.92.1's report branding). No iOS change. No manifest change. No `manifestSchemaVersion` bump.

---

