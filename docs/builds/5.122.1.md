## Build 5.122.1

* **Date:** 2026-06-09
* **PR:** [#156](https://github.com/murumoto-rgb/Forensic/pull/156)
* **Branch:** claude/roles-shared-types
* **Merge SHA:** 38a8950
* **Summary:** **Phase 3, PR 3.2** — shared wire types + zod request validators for users + roles + per-project assignment. Pure additions; zero behavior change. New types in `packages/shared/src/api.ts`: `ProjectRole = "admin" | "editor" | "viewer"`; `MeResponse { id, email, displayName, isAdmin }` (the source of truth for the web "am I admin?" gate; not admin-gated); `AdminUser { id, email, displayName, isAdmin, createdAt, pending, assignments }` + `AdminUserAssignment { projectId, projectName, role: "editor"|"viewer" }`; `ListAdminUsersResponse`; `InviteUserRequest/Response`; `PatchUserRequest/Response`; `SetProjectMembersRequest/Response` + `ProjectMemberAssignment { userId, role }`. `GetManifestResponse` gains optional `role?: ProjectRole` so the manifest GET response carries the caller's effective role (web's `useProjectManifest` will use it for `canEdit` in PR 3.5). New zod schemas in `validation.ts`: `InviteUserSchema`, `PatchUserSchema`, `SetProjectMembersSchema` — server route boundary validators for the upcoming admin API. The parity contract test's wire-only exemption set is extended for these three request validators (they have no iOS counterpart — the admin surface is web-only). 67/67 shared tests still pass; server + web typecheck clean. No manifest model touched → `MANIFEST_SCHEMA_VERSION` / `ios-models.json` untouched. No consumer code reads these types yet — the server access refactor (PR 3.3), admin API (PR 3.4), and web UI (PR 3.5) land them.

---
