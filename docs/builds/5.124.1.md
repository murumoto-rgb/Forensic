## Build 5.124.1

* **Date:** 2026-06-09
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/admin-api
* **Merge SHA:** TBD
* **Summary:** **Phase 3, PR 3.4** — the admin team-management API + the `GET /v1/me` identity endpoint that the web client gates on. **New `GET /v1/me`** (in `me.ts`, NOT admin-gated — every signed-in user calls it) → `{ id, email, displayName, isAdmin }`, source of truth for "should I show the admin link / page?". Falls back to JWT-derived identity if the `profiles` row hasn't committed yet (first-login race). **New `server/src/routes/admin.ts`** registered behind `authPlugin` + the `requireAdmin` preHandler from PR 3.3, so every `/v1/admin/*` route is admin-gated by construction. Five endpoints: `GET /v1/admin/users` lists all users with their org role + per-project assignments + a `pending` flag derived from `auth.users.last_sign_in_at === null` (badge for "Invited (pending)" in the UI); `POST /v1/admin/users/invite { email }` calls `supabaseAdmin.auth.admin.inviteUserByEmail` (no extra Supabase Management API integration needed) + does a defensive idempotent `profiles` upsert in case the `on_auth_user_created` trigger lags, returns 409 `already_invited` on a 422 from Supabase; `PATCH /v1/admin/users/:id { isAdmin }` flips the org-admin flag with a **last-admin lockout guard** (409 `last_admin` when removing admin would leave the org with zero admins); `DELETE /v1/admin/users/:id` revokes all memberships then hard-deletes via `supabaseAdmin.auth.admin.deleteUser` with guards: can't delete self (409 `self_delete`), can't delete a user who owns projects (409 `owns_projects` with hint), can't delete the last admin (409 `last_admin`); `PUT /v1/admin/projects/:projectId/members { members: [{ userId, role }] }` replaces the project's membership atomically (delete-then-insert) and silently excludes the project owner from the list (owner is implicit editor — never duplicated, per the data model). Helpers: `loadAdminUser(userId)` rebuilds an `AdminUser` DTO including assignments and last-sign-in for the invite/patch response. Wire types + zod from PR 3.2 (#5.122.1) consumed verbatim. 67/67 shared tests pass; server + web typecheck clean. No DB migration. No manifest model touched.

---
