## Build 5.57.1

* **Date:** 2026-06-06
* **PR:** [#87](https://github.com/murumoto-rgb/Forensic/pull/87)
* **Branch:** claude/sentry-posthog-observability
* **Merge SHA:** 1416b49
* **Summary:** Items **#4** and **#5** from the queue, bundled — Sentry crash reporting (web + server) and PostHog product analytics (web). Both SDKs initialise conditionally on env vars so dev + any deploy without keys set runs silently with no network noise. Server: new `@sentry/node` dep, new `server/src/sentry.ts` with `initSentry()` / `captureException` / `captureMessage` / `setRequestUser` helpers; `initSentry()` called at the top of `index.ts` BEFORE Fastify so auto-instrumentation hooks. Fastify error handler now captures only 5xx + uncaught throws (4xx skips Sentry's quota) and tags exceptions with `method`/`url`/`userId`. Auth middleware calls `setRequestUser(user.id)` so any later exception in the request carries user context. New env vars: `SENTRY_DSN`, `SENTRY_RELEASE` (defaults to `RENDER_GIT_COMMIT`), `SENTRY_TRACES_SAMPLE_RATE` (default 0.1). Web: new `@sentry/react` + `posthog-js` deps, new `web/src/lib/observability.ts` with `initObservability()` / `identifyUser` / `resetUser` / `trackEvent` / `captureException` helpers. `main.tsx` calls `initObservability()` before React mount. `App.tsx`'s session-change handler identifies on sign-in (Supabase UUID + email as person property) and resets on sign-out. PostHog autocapture covers page views + clicks; session recording disabled (privacy + noise). Neither SDK gets PII beyond user.id + email for identification. New env vars: `VITE_SENTRY_DSN`, `VITE_SENTRY_TRACES_SAMPLE_RATE`, `VITE_POSTHOG_KEY`, `VITE_POSTHOG_HOST` (defaults to `us.i.posthog.com`). `.env.local.example` for both packages updated. No iOS / shared / supabase migration / `manifestSchemaVersion` change. After merge, set `SENTRY_DSN` in Render env vars and `VITE_SENTRY_DSN` + `VITE_POSTHOG_KEY` in Vercel env vars — neither requires a redeploy beyond the env-var update.

---
