## Build 5.6.1

* **Date:** 2026-05-30
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/ios-isdeleted-tandem
* **Merge SHA:** TBD
* **Summary:** Complete the soft-delete-on-web fix end-to-end. PR #35 (Build #5.5.1) added a server filter on `manifest->>isDeleted` but neither the shared schema nor iOS's `Project` struct carried the field — so the filter had nothing to match. This PR is the tandem fix: adds `Project.isDeleted: boolean` to shared types + zod schema + iOS Codable struct, bumps `manifestSchemaVersion` to 2, and rewrites iOS `ProjectStore.delete(_:)`/`restore(_:)` to set the flag and call `save(_:)` (which triggers the manifest PUT via the existing SyncCoordinator) before the disk-folder move. Also extends the parity contract test's normalizer to handle `ZodDefault` (used for the back-compat default value on the validation schema). After this PR + iOS update via TestFlight, any new project deletion on iOS propagates to web within ~1 second.

---
