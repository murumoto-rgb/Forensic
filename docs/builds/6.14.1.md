## Build 6.14.1

* **Date:** 2026-06-10
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/review-code-visibility-izhPs
* **Merge SHA:** TBD
* **Summary:** **Web review Wave 2, PR 1: per-request memoization of `isOrgAdmin`.** Tracing the access-check hot path turned up a concrete redundancy on every elevated-role write: `assertProjectAccess(uid, projectId, "editor")` already calls `isOrgAdmin` internally; the freeze-flip path (`projects.ts:374, 486`) then re-queries `profiles` for the **same** admin status before letting the flag change, and the force-release path (`locks.ts:429`) does the same. The `requireAdmin` middleware that gates admin routes also re-queries when the handler asks again. None of this changes mid-request — the caller's admin bit is immutable for one HTTP turn. New per-request cache attached to the `FastifyRequest`: `isOrgAdmin(userId, request)` consults `request.__access.orgAdmin` first and only hits Postgres on a miss. `assertProjectAccess` gains an optional `request` parameter that forwards into the internal `isOrgAdmin` call, so every call site that threads `request` warms the cache for downstream code. **Sites threaded:** 16 `assertProjectAccess` call sites (every protected route — `projects.ts`, `aiTag.ts`, `files.ts`, `locks.ts`, `photos.ts`, `plans.ts`, `projectExports.ts`), plus the 5 explicit `isOrgAdmin` sites (list-endpoint priming in `projects.ts` + `files.ts`, the two freeze-flip checks in `projects.ts`, the force-release check in `locks.ts`, and `requireAdmin`). **Saves a DB round-trip on:** every editor PUT that flips the freeze flag, every admin route after the middleware accepted it, every force-release call (after #6.11.1's gate), and every list endpoint where a downstream check would otherwise re-query. **Back-compat:** the `request` parameter is optional in both signatures, so any call site I missed (or future code) keeps compiling — the behavior simply isn't optimized until threaded. Server typecheck clean; 68/68 shared tests pass; no behavior change observable to clients.

---
