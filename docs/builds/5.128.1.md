## Build 5.128.1

* **Date:** 2026-06-09
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/ios-tab-scope
* **Merge SHA:** TBD
* **Summary:** **iOS IA refactor — PR 2/7.** The four non-Photos tabs now show only the section that belongs to them instead of a "coming soon" placeholder. Implemented via a new `ProjectDetailScope` enum on `ProjectDetailView` so each tab renders a filtered slice of the same view: `.photos` → imports + photos list; `.plan` → floor plans; `.ai` → AI tagging; `.export` → export; `.more` → metadata + buckets; `.all` (the default) preserves today's full layout for any caller that doesn't opt in. Scope-aware FAB stack: Camera FAB on Photos + AI tabs; Distress FAB on Plan tab when plans exist; no FAB on Export / More. Scope-aware search bar via a new `SearchableIfPhotos` ViewModifier — `.searchable` only attaches on the Photos tab. The four extracted tabs each get their own `ProjectDetailView` instance, so each tab has independent `@State` for sheet visibility / filter chips / search text; `TabView` preserves that state across switches. **No code was deleted from `ProjectDetailView`** — every section, helper, sheet, and `@State` remains intact, just conditionally rendered. PRs 3-5 will replace each scope-filtered slice with a proper tab file; PR 6 deletes `ProjectDetailView` once every tab has its own implementation. No DB / shared / server / web touched; `MANIFEST_SCHEMA_VERSION` unchanged; parity contract test green.

---
