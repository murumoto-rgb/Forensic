## Build 5.131.1

* **Date:** 2026-06-09
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/settings-reshuffle
* **Merge SHA:** TBD
* **Summary:** **iOS Settings sheet reshuffle.** Reorganizes the 11 flat sections into three contiguous groups with visual dividers: **Personal · Team · Diagnostics**. Single section swap to make all three groups contiguous — Sync moves from position 2 (top) down to the start of the Diagnostics cluster; Appearance moves from position 8 (after Reports) up to the Personal cluster (right after Account). Net result: Personal contains Account + Appearance + API Key + AI Tagging Speed + Confidence Filter; Team contains AI Tagging Rules + Tag Library + Report Branding; Diagnostics contains Sync + Storage + Links + About. Three new banner `Section`s act as group dividers — each is a single `Label` with an SF Symbol and an explanatory footer. Sub-sheet wiring (`showingBranding`, `showingRulesTemplate`, `showingTagLibrary`) untouched. No code deleted; every existing setting still works with the same handler. No DB / shared / server / web touched; parity contract test green.

---
