## Build 6.15.1

* **Date:** 2026-06-10
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/review-code-visibility-izhPs
* **Merge SHA:** TBD
* **Summary:** **Web review Wave 2, PR 2: PDF-export poll timeout.** The original code review flagged "polling can get stuck forever" as a `ExportPdfControl` problem, and on closer inspection it's a real (if rare) bug: if a server-side render hangs in `running` (or crashes in a way that doesn't update the row to `failed`), the modal's spinner polls every 2 seconds indefinitely with no escape — until the user navigates away or refreshes. **Fix:** track a `pollStartedAtRef` wall-clock timestamp at every point the poll loop starts (a fresh `start()`, or a resume-from-localStorage on mount), and at the top of `poll()` bail with a user-readable error if the elapsed time exceeds **10 minutes**. The error explains that the render keeps running server-side (true — `clearPoll` only stops the client poll, the worker is independent) and points the user at the Exports page to see the final state. **Why 10 minutes:** the longest legitimate render I observed in `htmlReport.ts`'s 300-photo report tests was ~6 minutes on the Render free tier; 10 minutes gives slow legitimate jobs headroom while shaving off the genuinely-stuck case quickly. `clearPoll` resets the timestamp alongside the interval ref so a re-trigger gets a fresh budget. **Audit of the original finding's "orphaned interval" / "missing dependency" claims:** verified false. `start()` calls `clearPoll()` before assigning a new interval (line 261); the mount-resume effect runs exactly once per `projectId` and the unmount cleanup at `:169` clears it; `pollRef.current` is properly reset to null by `clearPoll`. The polling lifecycle is sound — the only real gap was the missing timeout, which is now fixed. **One-line behavior delta:** a stuck job that previously hung the modal forever now flips to an error after 10 minutes. Other paths unchanged. Web typecheck clean.

---
