## Build 6.22.1

* **Date:** 2026-06-10
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/review-code-visibility-izhPs
* **Merge SHA:** TBD
* **Summary:** **PhotoPreviewPanel split (Wave 4, item 2).** The web photo panel had grown to 1,614 lines holding four independent sub-features bolted onto one file — flagged in both review rounds as the web side's biggest maintainability debt. Split along the existing component boundaries (every moved component already had exactly one usage site in the shell, so the cut is purely mechanical): **`PhotoPreviewPanel.tsx` (620 lines)** keeps the shell — docking layout, nav scope, prev/next + arrow keys, thumb strip, URL preload cache; **`PhotoSuggestionsReview.tsx` (281)** owns the pending-suggestions review (accept/reject chips, bulk actions) plus its private `applySuggestionToPhoto` / `removeSuggestionFromPhoto` helpers; **`PhotoAIAnalysisViewer.tsx` (290)** owns the AI-analysis footer (`AIAnalysisViewer`, `AnalysisRow`, `AITagTree`, `nonTrivialString`); **`PhotoRetagControl.tsx` (225)** owns the single-photo Re-tag flow plus its private `mergeSuggestions` dedup helper; **`PhotoEditorSection.tsx` (226)** owns the caption/observation/bucket/tags/rotation/favorite editor. Helpers moved with their sole consumers (verified by grep before cutting: `mergeSuggestions` → retag only, `apply/removeSuggestionToPhoto` → review only, `nonTrivialString` → viewer only), docblocks traveled with their functions, and the shell imports the four exported components. Each file's import list was then minimized — the repo's `noUnusedLocals` tsconfig setting enforces this at compile time, which doubled as the verification that no moved code silently lost a dependency. **Zero behavior change:** the build output is the same workspace chunk (components were file-internal before; module boundaries don't alter the lazy-route chunking from #6.16.1). Typecheck + production build clean. Total line count is within 28 lines of the original (the delta is the per-file headers + import blocks).

---
