## Build 6.34.1

* **Date:** 2026-06-11
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/p2b-lock-control-admin-only
* **Merge SHA:** TBD
* **Summary:** **Hide Lock/Unlock control from editors on web (closes a P2 reviewer finding).** The Info tab showed the freeze toggle to any account with `manifest.role === "admin" || "editor"` (`web/src/components/workspace/InfoTab.tsx:36-37`), but the server gate on `PUT /v1/projects/:id` returns 403 unless the caller is an org admin OR the project owner (`server/src/routes/projects.ts:366-381`, established in #6.11.1's force-release tightening). An editor saw the button, tapped it, hit the 403. **Fix:** `canToggleFreeze = manifest.role === "admin"`. The docblock above the gate is updated to reflect what the server actually enforces (the prior copy was stale — it dated to before #6.11.1 when the gate said "tighten when roles land"). **Known small gap, documented:** an owner who isn't also an org admin no longer sees the control from web. They can still lock/unlock the same project from the iOS app's More-tab toggle (#6.1.1), and the change syncs to web, so they aren't locked out — they're just routed through iOS. The cleaner long-term fix is to surface per-project ownership on `/v1/me` or the manifest hook so the UI can show the control when `role === "admin" || role === "owner"`; that's logged in `docs/deferred-work.md` as a follow-on (≈half a day of server + UI). **Parity check:** no manifest model touched, no schema/zod change. The matrix's "Lock / finalize project" row already shows ✅ on both platforms (the iOS toggle is admin-AND-owner gated at sync time); no row edit needed. Web typecheck + production build verified in CI.
