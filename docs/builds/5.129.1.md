## Build 5.129.1

* **Date:** 2026-06-09
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/ios-buckets-tab
* **Merge SHA:** TBD
* **Summary:** **iOS IA refactor — tab layout adjustment (user feedback on #5.128.1).** Promotes **Buckets** to its own tab and folds **Export** into the **More** tab. New tab order: **Photos · Plan · AI · Buckets · More**. Rationale: bucket assignment is a frequent workflow, while export is a once-per-report action that fits naturally with the project-info / danger-zone settings on More. Implementation is a small enum swap: `ProjectTab.export` becomes `ProjectTab.buckets` (icon `folder.fill`); `ProjectDetailScope.export` becomes `ProjectDetailScope.buckets`; the scope-filter in `ProjectDetailView` body keeps `bucketsSection` under `.buckets` and adds `exportSection` to `.more` (instead of having buckets on More); `scopedFabStack` updated to render `EmptyView` on Buckets and More. `ProjectWorkspaceView` swaps the Export tab for Buckets. No code deleted; no behavior change beyond which tab each section lives on. No DB / shared / server / web touched; parity contract test green.

---
