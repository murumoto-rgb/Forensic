## Build 6.18.1

* **Date:** 2026-06-10
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/review-code-visibility-izhPs
* **Merge SHA:** TBD
* **Summary:** **Docs truth pass (second-review PR A).** Docs-only; no code change anywhere. (1) **PR-number backfill:** 157 of 165 build docs carried `[#XX] (to be assigned)` + `Merge SHA: TBD` placeholders that were never filled in. A script recovered the real PR numbers and short SHAs from the squash-commit subjects on `main` (`Build #N.M.P: … (#PR)`) and the merge-commit second-parent subjects for the older non-squash era, and rewrote all 157. Eight docs had no matching commit (abandoned branches / unnumbered commit messages) and keep their placeholders. The registry's audit trail is now navigable end to end. (2) **`docs/builds.md` numbering scheme rewritten** to match ~150 builds of actual practice: MAJOR = rare deliberate milestone (doubles as the marketing version since #6.0.0), BRANCH = the de-facto build counter (+1 per PR), PUSH = +1 per push within a branch. The old text described MAJOR bumping on every merge — never how the registry was used. Example timeline now mirrors real history. (3) **Parity-matrix "Phase status" section corrected** — it still claimed Phases 2–5 were 📋 planned; all are ✅ closed (2: Path P photos; 3: plans+distress; 4: AI/exports/freeze at #5.126.1; 5: server+web shipped, with only the iOS edit-lock UI open). (4) **"Start / stop project" row corrected**: the iOS ✅ was a doc error — both platforms have the schema fields, iOS auto-sets `startedAt`, but **no platform has a start/stop UI**. Now 🚧 (fields only) with a pointer to the backlog. (5) **`docs/deferred-work.md` gains the consolidated review backlog** — a 17-row table that supersedes the "Wave 4" notes scattered across build docs; the single source of truth for open review items from both audit rounds. (6) **Root `README.md` created** (monorepo overview, layout table, the parity rule, quick-start, deploy model — including the merge-≠-TestFlight distinction). (7) **`docs/phase-2-plan.md` marked HISTORICAL** with a banner pointing at the matrix + backlog. No DB / shared / server / web / iOS code touched; parity contract unaffected.

---
