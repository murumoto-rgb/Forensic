## Build 5.99.1

* **Date:** 2026-06-08
* **PR:** [#133](https://github.com/murumoto-rgb/Forensic/pull/133)
* **Branch:** claude/web-csv-export-modal
* **Merge SHA:** 01f9a97
* **Summary:** Round 3 PR #3 — full CSV export options modal. Replaces the inline trigger from PR #2 with a proper modal mirroring iOS's `AIAnalysisCSVExportRunner` controls: "Include only photos with AI analysis" checkbox (default on — matches iOS) + a confidence-threshold slider 0.0–1.0 in 0.05 increments (default 0.5 — matches iOS). New `web/src/components/CsvExportControl.tsx` follows the same shape as `FolderExportControl`. `ExportTab.tsx` swaps the inline component for the new one; the now-unused `useState` + `api`/`ApiError` imports + the `CsvExportInline` helper come out. Wire shape unchanged from PR #1 (still uses `kind: "csv"` + the existing options schema). No iOS change. No schema change. No `manifestSchemaVersion` bump.

---
