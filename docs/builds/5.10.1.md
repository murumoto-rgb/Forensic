## Build 5.10.1

* **Date:** 2026-05-31
* **PR:** [#40](https://github.com/murumoto-rgb/Forensic/pull/40)
* **Branch:** claude/sync-now-button-and-plan-toasts
* **Merge SHA:** a0e69f8
* **Summary:** Diagnostic + fallback for plan upload not happening on iOS Build #5.9.1. (a) PhotoSyncer's `uploadPlanIfNeeded` now toasts on every failure path (missing file, oversize, server error, etc.) instead of just printing to DEBUG, and adds detailed prints so iPhone Console.app shows the upload lifecycle. (b) New "Sync now" button in Settings → Sync section runs `pushAllToServer` + `syncAll` with start/end toasts. Auto-sync should still be the primary path; this exposes failures so we can fix the root cause.

---
