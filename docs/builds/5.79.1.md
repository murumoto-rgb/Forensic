## Build 5.79.1

* **Date:** 2026-06-08
* **PR:** [#113](https://github.com/murumoto-rgb/Forensic/pull/113)
* **Branch:** claude/photo-editor
* **Merge SHA:** 9778f31
* **Summary:** Path P #4/8 — per-photo editor. The Photos tab's row tag / locate / overflow buttons (previously disabled stubs) now open the docked `PhotoPreviewPanel` in edit mode against the clicked photo, with a new inline `PhotoEditorSection` that brings web up to iOS `PhotoTagEditorSheet` parity for editable fields: caption (writes `userCaption`), observation (writes `userObservation`), favorite toggle (writes `isFavorite`), bucket picker (writes `bucketID`; "— None —" clears), rotation cycle button (`previewRotation` 0 → 90 → 180 → 270 → 0), and direct tag management (chip list with ✕ remove per tag, plus an "Add a tag" input that commits on Enter). Text fields commit on blur — no per-keystroke PUT, but no debounce timer required either. Pre-existing AI machinery (accept/reject pending suggestions via `PendingSuggestionsReview`, single-photo `ReTagWithAIControl`, read-only `AIAnalysisViewer`) is reused unchanged; the editor section sits in the metadata footer alongside them, so one panel surface delivers the full per-photo workflow. Floor-plan users also gain editing — opening a photo from a pin click now exposes the same controls. `PhotoListRow` updates: tag / relocate / overflow buttons are no longer disabled stubs; they all route to `onOpenEditor(photo)`. `PhotoList` plumbs `onOpenEditor` through to each row. `PhotosTab` adds an `editorPhotoId` piece of state, opens `PhotoPreviewPanel` against it when set, and routes per-photo updates through the shared manifest hook so every change persists immediately. Relocate via pixel coordinates still happens on the Floor Plan tab's pin-drag UI — the editor surfaces the bucket / plan-assignment fields but the pin pixel coordinates remain a canvas-only operation. No schema change, no shared API change, no server change, no iOS change. No `manifestSchemaVersion` bump.

---
