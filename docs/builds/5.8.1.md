## Build 5.8.1

* **Date:** 2026-05-30
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/web-surface-plan-image-error
* **Merge SHA:** TBD
* **Summary:** Diagnostic — surface the underlying error message in the floor plan canvas "Failed to load plan image" banner instead of swallowing it. Earlier the user got the generic message with no hint of cause (network, CORS, or other pre-response failure). Now the banner shows e.g. "Failed to load plan image — TypeError: NetworkError when attempting to fetch resource" or whatever the real exception is. Web-only; no iOS or shared schema change.

---
