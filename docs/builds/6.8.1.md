## Build 6.8.1

* **Date:** 2026-06-10
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/review-code-visibility-izhPs
* **Merge SHA:** TBD
* **Summary:** **Plan-row menu consolidation (Wave 3, PR 2).** The per-plan ••• menu had grown to 8 flat items, and the "Edit Calibration Distance" vs "Re-calibrate Scale" pair was hard to tell apart mid-list. The menu now reads: **Rename · Set as active · Calibrate & Image ▸ · Remove** — with the five calibration-class actions (Set Origin, Set North, Edit Calibration Distance, Re-calibrate Scale, and Replace Image after a divider) inside the **Calibrate & Image** drill-in submenu (native SwiftUI nested `Menu`, the same drill-in pattern Apple's apps use). Every action's underlying flow, sheet, and store call is byte-identical — only its location in the menu moved, so there is zero regression surface beyond menu navigation itself. Also adds the missing "Plan actions" accessibility label to the icon-only ••• button. Deliberately chosen over the originally-sketched "single Calibrate sheet with internal tabs": the nested menu delivers the same decluttering with none of the new-chrome risk, and the existing per-task sheets (origin / north / re-calibrate / replace) remain independently reachable and unchanged. No DB / shared / server / web change; parity contract unaffected.

---
