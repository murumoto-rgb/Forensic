## Build 6.11.1

* **Date:** 2026-06-10
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/review-code-visibility-izhPs
* **Merge SHA:** TBD
* **Summary:** **Web review Wave 1, PR 1: lock force-release gated to Owner/Admin + parity-matrix truth pass.** (1) **The security gap:** `POST /v1/projects/:id/lock/force` was callable by **any signed-in user** — the route comment said "no admin role yet (Phase 5); tighten when roles land," but roles landed in #5.121–#5.125 and the gate was never added. A member-editor (or viewer) could kick the owner out of their editing session. Now requires `isOrgAdmin || isProjectOwner` — the same elevated-role rule as flipping `isFrozen` (#5.126.1) — returning 403 `forbidden` with a human-readable message. (2) **Web 403 handling:** `useProjectLock.force()` now returns a success boolean and `LockBanner` only chains the follow-up `acquire()` when the force landed — previously the acquire's re-fetch would instantly overwrite the error banner before the user could read it. A 403 shows the server's message verbatim ("Only the project owner or an org admin can force-release an edit lock."). (3) **Parity-matrix truth pass (the doc-drift cleanup):** ~18 web rows still claimed 📋 Planned / 🚧 for features that shipped long ago — verified against the code and corrected: plan view+editing, multiple plans, photo drag/re-locate, soft-delete/restore, photo groups, favorites, rotation, manual + hierarchical tags, confidence threshold, extra vocabulary, AI run + accept/reject, recommended-use + reviewer-flag display, all three distress rows, web PDF export, multi-user access (#5.125.1), and the edit-lock rows (web ✅ since #5.60.1; iOS lock UI correctly stays open). The "AI rules templates" row was doubly stale — iOS has synced that key since #5.36.1 and web's editor is at `/admin/ai-rules`. The "Calibrate scale / Set north" rows were clarified to mean *re-calibrating an existing plan* (genuinely iOS-only by design; on-add calibration shipped on web in #5.94.1). `docs/deferred-work.md` item 3 marked CLOSED. **Deploys live on merge** (Render redeploys the server; no TestFlight involved — iOS is untouched).

---
