## Build 6.19.1

* **Date:** 2026-06-10
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/review-code-visibility-izhPs
* **Merge SHA:** TBD
* **Summary:** **Small-wins bundle (second-review PR C): server AI-config cache + iOS freeze-flip toast.** (1) **`aiTag.ts` 60-second config cache.** Every server-driven `/v1/ai/tag-photo` request re-read `app_config` (tagLibrary + aiRulesTemplate) from Supabase — a 229-photo team-server batch issued 229 identical queries. New module-level cache with a 60s TTL: admin edits to the library/rules propagate within a minute (faster than a typical batch finishes), a failed or library-less fetch is never cached, and the single-process Render deployment means a plain module variable is the whole implementation. The 422 `config_missing` path still re-checks the DB each time (empty results aren't cached) so pushing a first library takes effect immediately. (2) **iOS toast on server-side freeze flips.** When a pull lands `isFrozen` changed by someone else (admin locked/unlocked the project from web), the device's banner used to just appear or disappear with no explanation. `applyServerProject` now compares the incoming flag against the in-memory copy and posts an info toast ("…was locked (finalized) from another device." / "…was unlocked from another device.") — using the established `Task { @MainActor }` hop since `ToastCenter` is MainActor-isolated and `ProjectStore` is not (the #6.10.1 lesson, applied up front this time). Pull adoption behavior is otherwise byte-identical; the toast fires before the disk write but doesn't gate it. (3) **Camera orientation "race" from the second-pass audit: verified false and skipped.** `updateOrientation(initial: true)` reads `UIDevice.current.orientation` directly after `configure()` completes, so a rotation during camera startup is already captured by the post-configure snapshot — the third false alarm from this audit round (after the ManifestSyncer dirty-flag and markup-thumbnail claims). Server typecheck clean; iOS change mental-compiled against the corrected isolation facts.

---
