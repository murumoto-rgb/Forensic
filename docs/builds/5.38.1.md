## Build 5.38.1

* **Date:** 2026-06-04
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/prompt-compiler-snapshot-test
* **Merge SHA:** TBD
* **Summary:** Sprint A1 of the AI-tagging completion plan — snapshot-based parity test for `compilePrompt` + `compileUserPrompt`. New `packages/shared/tests/promptCompiler.test.ts` builds a small fixture project + tag library that exercises every branch of the compiler (two contexts in selection, a primary with all secondaries deselected, a primary with a literal "None" sentinel needing filter, a primary with no secondaries at all to trigger the `(primary alone)` placeholder, `aiInstructions` notes block, `aiExtraVocabulary` extras block with both secondaries-present and secondaries-absent branches) and asserts byte-for-byte equality against checked-in expected strings written as template literals inline. Tests cover: five-block emission with correct cacheable flags, joined-prompt concatenation, `PromptCompileError` on null selection AND on entirely-stale selection (every context ID missing from library), notes/extras block omission when absent, and `compileUserPrompt` with / without a photo ID. `pnpm parity` script extended to run both test files; root `pnpm parity` already invokes the shared package's test, so CI's existing parity workflow picks it up automatically. No production code change. The expected strings — preamble, vocabulary header, "(primary alone)" placeholder, INVESTIGATION CONTEXT extras heading, output contract — are the canonical contract; iOS and TS must produce identical output for these. To regenerate after a deliberate prompt change: edit iOS + TS together, run iOS's `PromptCompiler.compile(...)` with the fixture, paste the new blocks into the test. Lays the foundation Sprint B / C / D / E build on.

---
