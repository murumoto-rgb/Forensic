## Build 5.52.1

* **Date:** 2026-06-06
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/anthropic-key-team-server-state
* **Merge SHA:** TBD
* **Summary:** Anthropic API Key Settings section reacts to the "Use team server for AI" toggle. When the toggle is ON, the SecureField + Save / Remove buttons are replaced with a green `checkmark.seal.fill` "Using team server — no device key needed" state; the section footer explains the device key isn't used in this mode and that a stored key (if present) is preserved for if the user flips the toggle back off. When the toggle is OFF, the section renders exactly as before (editable key field + Save / Remove + "Key on file" / "Saved to Keychain" indicators). The stored Keychain key is never cleared by the toggle — flipping team-server back off restores the device-key path without re-entry. Also adds `docs/deferred-work.md` recording the cloud-first storage idea (offload projects to R2, download on demand) as a deferred Phase 5/6 item, with scope + trade-off notes, per the user's "keep hybrid, defer cloud-first" decision. iOS-only PR (plus a docs file). No server / web / shared / supabase change.

---
