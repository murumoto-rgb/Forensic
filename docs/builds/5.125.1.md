## Build 5.125.1

* **Date:** 2026-06-09
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/web-admin-ui
* **Merge SHA:** TBD
* **Summary:** **Phase 3, PR 3.5** — the closing piece of Phase 3. After this lands, Org Admins manage users + per-project assignments entirely in the browser. **`useProjectManifest`** stores the `role` from `GetManifestResponse` (added in #5.122.1, populated server-side in #5.123.1) and exposes `role` + a derived `hasWriteAccess = role !== "viewer"` (null/admin/editor pass). **`ProjectWorkspacePage`** folds it into the canonical `canEdit = lock.status === "holding" && manifest.hasWriteAccess`, so a viewer cannot edit even when holding the lock — the server would 403 every write anyway, but the UI gating keeps the lock semantics clean. **New `web/src/pages/AdminUsersPage.tsx`** at `/admin/users` (session-gated in `App.tsx` like the other admin pages; in-page also calls `api.getMe()` and renders a "Not authorized" panel for non-admins; server 403s every `/v1/admin/*` route regardless). Users table shows email, "Admin" / "Invited (pending)" / "You" chips, role + assignment summary (or "Admin — access to every project"), and per-row Make admin / Demote admin / Remove buttons. Remove requires a confirm dialog. Self-row's Remove is disabled. The invite form sends a Supabase magic-link email; success/failure surfaces inline; on success the list reloads so the new "Invited (pending)" row appears immediately. **`SettingsPage` `TeamConfigSection`** gains a `Team & access →` link shown **only when `getMe().isAdmin === true`** (other admin links still visible to everyone today — Phase 5 may admin-gate them later). **`InfoTab` `MembersSection`** (new, admin-only — auto-hides for non-admins via the `isAdmin === null/false` early return): lists every non-admin user with three role buttons per row (None / Viewer / Editor); flipping a role fires `PUT /v1/admin/projects/:id/members` immediately with the new full member list (full-replace; idempotent); admins always see every project so they're omitted from the picker. New API methods in `web/src/lib/api.ts`: `getMe`, `listAdminUsers`, `inviteUser`, `setUserAdmin`, `removeUser`, `setProjectMembers`. `ProjectListItem` gains an optional `role?: ProjectRole`. 67/67 shared tests pass; server + web typecheck clean. No DB migration. No manifest model touched. **Phase 3 closed** — invite users via UI; assign them per-project; viewers see read-only; admins see everything.

---
