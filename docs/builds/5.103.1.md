## Build 5.103.1

* **Date:** 2026-06-08
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/web-floorplan-polish
* **Merge SHA:** TBD
* **Summary:** Round 3 PR #7 — floor-plan polish. **Plan reorder** in `FloorPlanManager.tsx` via ↑/↓ buttons per row. Manifests store plans in array order on both iOS and web; no new `sortOrder` field needed (avoids a manifest schema change). Disabled at array ends. **Persistent bubble scale** in `FloorPlanTab.tsx` — `bubbleScale` state now hydrates from + writes to `localStorage["sitephoto.planBubbleScale"]` so the user's chosen pin size survives navigation + reload. Cross-device sync via `useUserPrefs` is a small follow-on. **Plan color mode picker** (status / bucket / primaryTag) is deferred from this PR — implementing alternative color modes requires invasive changes to the `FloorPlanCanvas` pin renderer; for now web pins continue to color by bucket (current behavior). Tracked as a Round 3 follow-on. No iOS change. No schema change. No `manifestSchemaVersion` bump.

---
