## Build 6.7.1

* **Date:** 2026-06-10
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/review-code-visibility-izhPs
* **Merge SHA:** TBD
* **Summary:** **UX quick-wins (Wave 3, PR 1).** (1) **Truthful empty-filter state.** When filters exclude every photo, the list said "No photos match the selected tag filter." regardless of which filter did the excluding (favorites-only with zero favorites blamed a tag filter). It now names the active filters via the new `PhotoFilterState.summaryDescription` — e.g. "No photos match: tags (Crack, Spalling) · favorites · date (Today)." — and offers a one-tap **"Clear all filters"** button (resets the consolidated filter struct, including search text). (2) **Dark-mode toast colors.** `ToastBanner`'s info/error backgrounds were hardcoded light-mode RGB values (`0.10/0.40/0.70` blue, `0.78/0.20/0.20` red); they now use system `Color.blue`/`Color.red` with opacity — adapting to dark mode exactly like the green/orange that success/warning already used. (3) **Accessibility labels** on the two icon-only filter pills that lacked them (location pill → "Filter by location", level pill → "Filter by level"). **Scope note — three planned quick-wins found already shipped on inspection and skipped:** PhotoRow's icon buttons already carry accessibility labels, `BucketPickerSheet` already assigns-and-dismisses in one tap, and `PhotoTagEditorSheet` already shows a "No pending suggestions" empty state — the review audit predated PRs #161–165 which added them. No DB / shared / server / web change; parity contract unaffected.

---
