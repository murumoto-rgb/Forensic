## Build 5.87.1

* **Date:** 2026-06-08
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/web-bucket-drag-reorder
* **Merge SHA:** TBD
* **Summary:** Drag-and-drop bucket reorder on web — closes the small Path P #5/8 follow-on. `BucketManager.tsx` grows a per-row "⋮⋮" grip handle that's `draggable` when the user holds the edit lock. Drag state lives in two new pieces of local state (`draggingId`, `hoverIdx`); the row currently being dragged dims to 50% opacity, the hovered drop target highlights with a blue border + blue-tinted background. On drop, the new `reorderByDrop` helper splices the dragged bucket into the new index and renumbers `sortOrder` contiguously from 0 — keeping the field tidy after many drops and leaving the existing ↑ / ↓ swap buttons working for keyboard users. The future-proof "preserve buckets not in the sorted list" guard is dormant today (every bucket is in the list) but harmless. Browser HTML5 DnD API only — no new dependency. `setData("text/plain", id)` is set for Firefox's drag-start requirement; `effectAllowed = "move"` keeps the cursor sane. No schema change, no shared API change, no iOS change. No `manifestSchemaVersion` bump.

---
