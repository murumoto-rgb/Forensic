## Build 5.5.1

* **Date:** 2026-05-30
* **PR:** [#35](https://github.com/murumoto-rgb/Forensic/pull/35)
* **Branch:** claude/server-filter-deleted-projects
* **Merge SHA:** e4227b7
* **Summary:** Fix web project list showing iOS-deleted projects. `GET /v1/projects` now filters via `manifest->>isDeleted` JSONB path (keeps rows where the field is null or false; drops rows where it's true). `GET /v1/projects/:id` matches: returns 404 for soft-deleted projects so deep-linking a deleted URL also fails. No iOS or web changes; no schema migration; the `projects.is_deleted` column stays unused for now (the manifest PUT path doesn't extract it, and JSONB filtering is simpler).

---
