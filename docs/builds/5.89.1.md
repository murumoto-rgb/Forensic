## Build 5.89.1

* **Date:** 2026-06-08
* **PR:** [#123](https://github.com/murumoto-rgb/Forensic/pull/123)
* **Branch:** claude/web-floor-plan-management
* **Merge SHA:** 1a8861b
* **Summary:** Floor plan management on web — half of the "floor plan management" follow-on (the manifest-only half). New `FloorPlanManager.tsx` renders above the existing plan-picker chip row on the Floor Plan tab. Defaults collapsed so the canvas keeps its prominence; expanded view shows one row per plan with: an inline label input (commit on blur — same pattern as the project name + address inputs), a "Set active" button (writes `Project.activeFloorPlanID`), and a "Remove" button (confirms with a photo-impact preview, drops the plan from `Project.floorPlans`, nulls `floorPlanID` + `planPixelX/Y` on every photo that referenced the removed plan, and falls back to the first remaining plan as active if the removed one was active). The Remove button is disabled when there's only one plan (the canvas needs at least one to render). Per-row photo count and an "Active" pill round out the row. The "+ Add floor plan" affordance is deliberately deferred — it needs image upload + a calibration sheet and is bundled into the file-upload PR. No schema change, no shared API change, no iOS change. No `manifestSchemaVersion` bump.

---
