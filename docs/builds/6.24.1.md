## Build 6.24.1

* **Date:** 2026-06-10
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/review-code-visibility-izhPs
* **Merge SHA:** TBD
* **Summary:** **ProjectDetailView decomposition, part 2 of 2 (Wave 4) — the per-tab section builders move into same-module extensions.** Following part 1's support-struct extraction (#6.23.1), the three big contiguous member groups leave the main file: **`ProjectDetailView+PlanSection.swift` (203)** — `floorPlanSection`, `planRow` (with the #6.8.1 Calibrate & Image submenu), subtitles, `commitEditDistance`, `planThumbnail`; **`ProjectDetailView+PhotosSection.swift` (909)** — `photosSection`, trash section + rows + actions, `photosSectionHeader`, selection mode, `filteredPhotos` (with the #6.5.1 memo), all eight filter menus/chips/bar, search matching, needs-review/validation/measurement predicates, `bucketsInUseFor`; **`ProjectDetailView+AISection.swift` (377)** — `aiTaggingSection` (with the #6.9.1 guardrail), cost estimate, the four subtitle helpers, `presentSelectedAITagPrompt`, `startBatchTagging` → `retryFailedTagging` (the full batch machinery; this file imports UIKit for the background-task API). **`ProjectDetailView.swift` lands at 1,398 lines** — body, scope plumbing, More-tab sections, FABs, import/capture flows, and state declarations. **The access-level change this requires:** Swift scopes `private` to the *file*, so the struct's members that now cross file boundaries dropped `private` (a scripted sweep over `func`/`var`/`struct`/`enum`/`static` declarations, same-module visibility only — nothing is exposed outside the app target). Two deliberate exceptions kept `private`: `readFileForImport` (callers stayed in main) — and one sweep gap was caught in self-review: `FilterMemoBox` is a `final class` (the sweep regex missed `class`), and `filteredPhotos` in the Photos extension touches its members, so it was flipped by hand. **Verification without a compiler:** per-file brace balance (276/276, 46/46, 254/254, 82/82), comment-aware cuts (docblocks travel with declarations), the three AI sub-ranges glued and seam-checked, `scopedSections`' five cross-file references confirmed, and a final grep proved no remaining `private` member is referenced from an extension file. **Cumulative effect of the two parts: 3,442 → 1,398-line main file (−59%)**, with each tab's content in its own independently type-checked file — the structural goal the IA refactor deferred at #5.128.1, and the practical mitigation for the Build-42 "expression too complex" class. No behavior change intended. No DB / shared / server / web change.

---
