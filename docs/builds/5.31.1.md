## Build 5.31.1

* **Date:** 2026-06-02
* **PR:** [#61](https://github.com/murumoto-rgb/Forensic/pull/61)
* **Branch:** claude/highlight-group-primary
* **Merge SHA:** 926691b
* **Summary:** Follow-up to #5.30.1: when a non-primary group member (a "reshoot" surfaced via Group nav in the preview) is selected, the canvas now highlights the GROUP'S PRIMARY pin in amber — the same pin the recenter effect anchors on. Previously the highlight tried to match the non-primary's id, but non-primaries don't render their own pin (filtered out of `planPins`), so no pin would appear highlighted at all. Resolution helper in `ProjectPlanPage.tsx` mirrors the anchor fallback logic from #5.30.1. Web-only; no iOS, server, shared, or schema change.

---
