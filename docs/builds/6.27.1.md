## Build 6.27.1

* **Date:** 2026-06-10
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/review-code-visibility-izhPs
* **Merge SHA:** TBD
* **Summary:** **Fixes the Xcode build failure introduced by #6.24.1's decomposition splitter.** Root cause: the comment-aware splitter walked declarations' *docblocks* back so they traveled with their functions — but not **attribute lines**. Three boundary declarations had `@ViewBuilder` on its own line above the `func`: the attribute got stranded at the tail of the *preceding* block while the moved function lost it. Symptoms matched exactly: orphaned `@ViewBuilder` dangling before `ProjectDetailView+PlanSection.swift`'s closing brace ("Expected declaration"), `photosSection` failing opaque-return-type inference in `+PhotosSection.swift` (a multi-statement `some View` body needs the builder), a second orphan at `+PhotosSection.swift`'s tail, and a mid-file orphan in `+AISection.swift` sitting above the String-returning `estimatedCostString` (where it would have been invalid anyway). **Fix:** deleted the three stranded attributes and restored `@ViewBuilder` on `photosSection` and `aiTaggingSection`. `floorPlanSection` and `planThumbnail` are single-expression bodies and correctly remain attribute-free (matching their pre-split form). **Systematic re-verification, not just the visible errors:** a scanner now checks every `@ViewBuilder` in all four files is followed by a declaration (zero orphans), every multi-statement `some View` function carries the attribute (the two flagged singles are legitimate), and per-file brace balance holds. Main (`ProjectDetailView.swift`) needed no changes — its boundary declarations kept their attributes. **Lesson recorded for the next split:** attribute lines (`@ViewBuilder`, `@MainActor`, `@discardableResult`, …) are part of a declaration exactly like its docblock and must travel with it — the splitter's walk-back must include them. This is the fourth Xcode-only failure class this program has hit (enum drift #5.132.1, actor isolation #6.10.1, and now attribute stranding) — all from the same root: CI doesn't compile Swift. No behavior change; pure parse-level repair.

---
