## Build 5.132.1

* **Date:** 2026-06-09
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/ios-fix-typecheck
* **Merge SHA:** TBD
* **Summary:** **Fixes the Xcode Cloud build-42 archive failure** (3 Swift errors that CI couldn't catch — CI doesn't compile Swift). **Two real bugs:** (1) **`ProjectDetailScope` enum drift** — PR #163 (Build #5.129.1) intended to rename `case export` → `case buckets`, but the enum edit landed in a batch that hit a "file modified since read" race and only the *body* + *FAB* edits were re-applied; the enum definition kept `case export`. So `ProjectTab`, `ProjectWorkspaceView`, and the body's scope conditionals all referenced `.buckets` against an enum that didn't have it → "Type 'ProjectDetailScope' has no member 'buckets'" at `ProjectWorkspaceView.swift:89` and `ProjectDetailView.swift:789`. Fixed by correcting the enum to `case buckets` / `case more` (matching `ProjectTab` and every call site). (2) **"Expression too complex"** at `ProjectDetailView.swift:280` — the `List { }` literal held seven chained `if scope == … { section() }` branches inline, inside `Group { ScrollViewReader { List { … } } }`, forcing the whole `body` to type-check as one expression. Extracted the conditionals into a dedicated `@ViewBuilder private func scopedSections(_:)` with precomputed scope booleans, giving the type checker a small independent context. Net: all three archive errors clear. No behavior change — same sections, same tabs, same FAB logic. No DB / shared / server / web touched; parity contract green (68/68). **Process note:** this class of error (Swift type/availability mistakes) only surfaces in an actual Xcode build, never in this repo's CI. Self-review must mentally compile enum-exhaustiveness + ViewBuilder complexity for every iOS PR.

---
