## Build 5.130.1

* **Date:** 2026-06-09
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/ios-ai-3groups
* **Merge SHA:** TBD
* **Summary:** **iOS AI tab visual reorganization.** Splits today's monolithic AI Tagging section into three visual subgroups so a user can find what they're looking for at a glance: **Vocabulary** (AI Tags + Job-Specific Vocabulary), **Notes** (AI Notes), **Run** (Auto-tag untagged + Auto-tag every + Clear AI tagging). Each subgroup is now its own `Section` with header + explanatory footer. Removed the misplaced "Filter photos by tag…" button (filtering already lives in the photos-section filter chip bar; it had no business sitting in the AI configuration section). The `showingTagFilter` `@State` + sheet binding become dead code; they survive untouched in `ProjectDetailView.swift` because cleaning them up is part of the deferred PR 6 file-deletion work. No code deleted (one button removed, three Sections in place of one); no behavior change to any button's action — every sheet that opened today still opens with the same handler. No DB / shared / server / web touched; parity contract test green.

---
