## Build 5.80.1

* **Date:** 2026-06-08
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/bucket-manager
* **Merge SHA:** TBD
* **Summary:** Path P #5/8 — full bucket CRUD on the web Buckets tab. New `web/src/components/BucketManager.tsx` (245 lines) replaces the read-only listing the tab carried since #5.76.1. Surfaces: a "Create bucket" row at the top (name input + 9-color swatch palette + Add button; Enter commits), a per-bucket row for every existing bucket (color swatch picker · name input editable in place with commit-on-blur · live photo count · up/down move buttons that swap `sortOrder` with the neighbor · delete button with a confirm dialog that surfaces the affected-photo count), and a footer "Unbucketed: N photos" so the user knows the unassigned pool size. Color palette is a fixed 9-swatch set (red / orange / yellow / green / cyan / blue / violet / pink / neutral); selected swatch carries a white ring. Every mutation routes through `manifest.save(nextProject)` so writes happen via the shared optimistic-save loop with revision tracking. Delete behavior: removes the bucket from `buckets[]` AND nulls `bucketID` on every photo that referenced it (no dangling assignments). Reorder behavior: swaps `sortOrder` with the adjacent bucket (drag-and-drop deferred — arrows match the iOS UX and avoid the touch-vs-mouse complexity). `BucketsTab` shrinks from a read-only summary to a header + `BucketManager`; per-photo bucket assignment continues to live on the photo editor (Photos tab → 🏷 → Bucket select). All client-side over the manifest. No new API, no schema change, no shared API change, no server change, no iOS change. No `manifestSchemaVersion` bump.

---
