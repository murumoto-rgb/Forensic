## Build 5.65.1

* **Date:** 2026-06-07
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/pdf-cap-1000
* **Merge SHA:** TBD
* **Summary:** Raise `MAX_PHOTOS_PER_PDF` from 200 → 1000 after office staff hit the 200 limit on a real 525-photo Sarreon 2 project, and add `process.memoryUsage().rss` logging at three points around the Puppeteer render (pre-render with HTML byte count, post-setContent, post-pdf with delta + output size) so the next adjustment to the cap is measured rather than guessed. The header comment on the constant points at this PR + the pdfWorker's instrumentation so a future tuner has the full context. At 1000 thumbs × ~30 KB base64 the HTML is ~30 MB and Chromium's parsed DOM is several × that — we're explicitly near Render free-tier 512 MB ceiling. Expected outcome on the 525-photo case: works. Stress case (1000-photo project): may OOM the container; if so, the Render Events tab will show a restart and the log lines from this PR will tell us exactly which step blew the budget. Two paths if OOMs happen: Starter tier upgrade (~$7/mo, 2 GB) or implement chunked rendering (multi-day effort to render in batches and merge PDFs server-side). Server only — no schema change, no shared/iOS/web change. No `manifestSchemaVersion` bump.

---
