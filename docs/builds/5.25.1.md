## Build 5.25.1

* **Date:** 2026-06-02
* **PR:** [#55](https://github.com/murumoto-rgb/Forensic/pull/55)
* **Branch:** claude/claudemd-manual-testflight
* **Merge SHA:** 4ec2c26
* **Summary:** Documentation correctness — update CLAUDE.md to reflect that TestFlight is no longer auto-triggered on merge to `main`. The Xcode Cloud workflow's start condition is now "Tag Changes matching `ios-release*`" (configured in App Store Connect this session); a merge to `main` deploys Vercel + Render but does NOT fire an iOS build. Adds a new "Merging is just promote-to-main, NOT a TestFlight delivery" subsection with the two manual-trigger paths (dashboard Start Build, or `git tag ios-release-<n>` push) and a reminder not to prompt for TestFlight on web/server-only merges. No code change. Doc-only PR.

---
