## Build 6.0.0

* **Date:** 2026-06-09
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/review-code-visibility-izhPs
* **Merge SHA:** TBD
* **Summary:** **Sets the main version number to 6.0.0.** This repo carries two distinct version numbers and both are now aligned at `6.0.0`: (1) the **marketing version** — `CFBundleShortVersionString` in `ios/project.yml` (the xcodegen source of truth for `Info.plist`), bumped from the stale `"0.1.0"` to `"6.0.0"`. This is the version TestFlight and the App Store display. (`CFBundleVersion` stays `"1"` locally; Xcode Cloud overrides it per workflow run via `ios/ci_scripts/ci_post_clone.sh`, so it is left untouched.) (2) the **in-app build number** — derived by `ios/scripts/gen-build-info.sh`, which takes the version-sorted maximum filename in `docs/builds/*.md` and embeds it in the gitignored `BuildInfo.swift` (shown in the in-app "About" section). Adding this `6.0.0.md` file makes it the new sort maximum (`sort -t. -k1,1n -k2,2n -k3,3n` puts `6.0.0` above `5.134.1`), so the About section now reads `#6.0.0`. No `gen-build-info.sh` change is needed — the per-component numeric sort already orders `6.0.0 > 5.134.1` correctly. No application code touched: `ios/project.yml` + this build doc only. No DB / shared / server / web change; parity contract unaffected.

---
