## Build 5.102.1

* **Date:** 2026-06-08
* **PR:** [#136](https://github.com/murumoto-rgb/Forensic/pull/136)
* **Branch:** claude/web-photo-rotation-renumber-compare
* **Merge SHA:** 8e9dd01
* **Summary:** Round 3 PR #6 — photo group reshoot comparison + renumber-by-date. New `PhotoComparisonView.tsx` is a two-pane carousel for grouped photos: primary first, reshoots after in `sequenceNumber` order. Reachable from the preview panel's new "Reshoots" card (which only renders when the focused photo has a non-null `groupID` and the group has >1 members). The two panes show the active photo + the next photo in the group; sequence-number chips above let the user jump to any member. Image URLs fetch lazily via `api.getPhotoImageUrl` per pane (5-minute presigned-GETs). Honors `previewRotation` via CSS transform. New "Tools" section on the Info tab adds a "Renumber photos by date" action — confirms with a dialog, sorts `project.photos` ascending by timestamp, reassigns `sequenceNumber` 1..N, manifest.save. Web doesn't rename on-disk files (unlike iOS) since photo filenames here are `<uuid>.<ext>` blobs; only manifest changes. Photo preview rotation was already implemented (Build #5.79.1's editor section), so no work there. Group/all preview-nav mode toggle was already in `PhotoPreviewPanel` via `scopedPhotoIds` from FloorPlanCanvas cluster taps — no UI change needed for this round. No iOS change. No schema change. No `manifestSchemaVersion` bump.

---
