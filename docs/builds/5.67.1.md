## Build 5.67.1

* **Date:** 2026-06-07
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/pdf-options-parity-types
* **Merge SHA:** TBD
* **Summary:** Path C #2/8 — foundation. Extends the web `PdfExportOptions` shared type to mirror iOS `PDFExportOptions` field-for-field (1:1 capability parity per CLAUDE.md is the goal of the Path C work; this PR is the type plumbing the next five renderer PRs need to reference). Renderer logic still uses the legacy #5.64.1 fields — those are kept as `@deprecated` optionals on the type — so this PR is byte-equivalent on the wire output. Subsequent PRs (#5.68.1 – #5.71.1) plumb each new field through the renderer one at a time; PR #5.73.1 swaps the web UI to set the new fields, retiring the legacy ones. New types in `packages/shared/src/api.ts`: `PdfPlanMode` (`photoOnly`/`distressOnly`/`photoAndDistressSeparate`/`merged`, mirrors iOS `PlanRenderMode`), `PdfSection` (`plan`/`contactSheets`/`metadataTable`, mirrors iOS `Section`), `PdfAnnotationOptions` (5 independent booleans for tags/caption/observation/measurement/reviewerFlag, mirrors iOS `AnnotationOptions`). New required fields on `PdfExportOptions`: `perPage: number` (iOS-style flexible Int, default 6), `groupByBucket`, `includeMetadataTable`, `annotations`, `sectionOrder`, `selectedFloorIds: string[] | null` (multi-select, `null` = all), `planMode`. Legacy fields (`photosPerPage: 1|2|4`, `photoFilter`, `floorPlanId`, `includeFloorPlanPages`) become optional + deprecated. Server defaults helper extended: `DEFAULT_PDF_ANNOTATIONS` matches `AnnotationOptions.defaults` from iOS; `DEFAULT_PDF_EXPORT_OPTIONS` matches iOS `PDFExportOptions.defaults`; `applyOptionDefaults` is total with sensible legacy-field defaults so renderer output during the rollout stays identical to #5.64.1. Server zod schema accepts both new + legacy fields, all optional. Renderer reads new `perPage` first (clamped to 1/2/4 by the existing CSS grid; iOS-style flexible perPage layout lands in PR #5.68.1). Web `ExportPdfControl` `DEFAULT_OPTIONS` extended with iOS-default values for the new fields so the type checks; modal UI rewrite is PR #5.73.1. No schema change (JSONB column on `pdf_export_jobs.options` accepts the new shape transparently). No iOS change. Server + shared + web (types only). No `manifestSchemaVersion` bump.

---
