## Build 5.41.1

* **Date:** 2026-06-04
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/web-suggest-accept-reject
* **Merge SHA:** TBD
* **Summary:** Sprint C1 of the AI-tagging completion plan — suggest/accept/reject pipeline on web. The Re-tag button now writes the model's output through `Photo.pendingSuggestions` (matching iOS's mature flow) instead of straight into the manifest, and the panel renders a review UI where the user accepts or rejects each chip before it lands in `Photo.tags`. New shared `aiAnalysisToSuggestions(analysis)` ports iOS's `ClaudeTaggingService.suggestions(from:)` byte-for-byte: 0.99 sentinel confidence for primaries, per-tag confidence lookup (case-insensitive, lowercase-keyed) for secondaries with 0.7 fallback when missing, "none" sentinel filter, leading "N. " number stripper, synthetic `MEASUREMENT_TAG_LABEL` tag when `measurementVisible` is non-empty. New `mergeSuggestions(existing, incoming)` dedups by (label, parentTag) so a second Re-tag pass doesn't double the chips — same `setPendingSuggestions` discipline iOS applies. New `applySuggestionToPhoto` / `removeSuggestionFromPhoto` immutable helpers — accept moves a suggestion into `Photo.tags` (dedups by label + parentTag) and clears it from `pendingSuggestions`; reject just clears it. New `PendingSuggestionsReview` + `SuggestionGroup` + `SuggestionChip` components in `PhotoPreviewPanel.tsx` render the suggestions grouped by primary (with synthesised headers when secondaries arrive without their parent in the suggestion list), each chip showing the confidence percentage with ✓ / ✕ buttons. Header carries bulk "Accept all" / "Reject all" actions. Confidence threshold from #5.40.1 visually dims (opacity 0.6) below-threshold suggestions but still renders them so the user can choose to accept anything below the slider. Snapshot test `tests/aiSuggestions.test.ts` (8 cases, 30 total parity assertions now) locks in primary + secondary order, confidence shape, "none" filter, "N. " stripper, measurement-reading synthesis, conf-clamp to [0, 1], and the case-insensitive primary-secondary key match. Web-only PR. No iOS, server, shared schema, or `manifestSchemaVersion` change.

---
