## Build 5.95.1

* **Date:** 2026-06-08
* **PR:** [#129](https://github.com/murumoto-rgb/Forensic/pull/129)
* **Branch:** claude/web-server-user-prefs
* **Merge SHA:** 19d908d
* **Summary:** Server-backed per-user preferences — closes the 11-PR plan's last item. New `user_prefs` table (migration 0010) keyed by `auth.users(id)` with a flat JSON blob + revision token + RLS read policy. New `meRoute` (`server/src/routes/me.ts`) exposes `GET /v1/me/prefs` (synthesizes a 200 with empty shape + empty revision for first-time reads) and `PUT /v1/me/prefs` (zod-validated, revision-token optimistic concurrency mirroring the app_config pattern). New shared `UserPrefs` interface + zod schema + request/response types live in `packages/shared`. `useUserPrefs` now hydrates from the server once per app load (module-scoped flag — multiple mount points share the same hydration), merging server-side values into localStorage ONLY for fields the user hadn't already set locally (defensive — never clobber a local choice with a stale server value). Setter writes flow through both layers: localStorage (instant, source of truth for hot reads) + a 1-second debounced `PUT /v1/me/prefs`. Cross-tab sync still uses the storage event + custom-event pair from #5.85.1. Network / 409 failures are non-fatal — localStorage stays canonical for the session; next mount retries. Parity test scopes `UserPrefs` into `APP_CONFIG_STRUCTS` (it's web-only today; iOS-side prefs live in UserDefaults). No iOS change. No `manifestSchemaVersion` bump (this lives in `user_prefs`, not the project manifest).

---
