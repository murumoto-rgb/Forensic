## Build 5.17.1

* **Date:** 2026-06-01
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/server-cold-start-fixes
* **Merge SHA:** TBD
* **Summary:** Eliminate the Render free-tier cold-start problem (initial page load is slow; first PUT/GET often fails with "Couldn't reach the server" / "Failed to load project"). Two-part fix: (1) GitHub Actions cron workflow pings `/healthz` every 14 min so the server never goes idle past the 15-min spin-down threshold — needs a one-time `RENDER_HEALTHZ_URL` repo secret to activate. (2) The web API client's `request<T>` helper now retries every call (not just PUT manifest) up to 5 times with exponential backoff (2s → 5s → 10s → 15s → 25s = ~57 sec ceiling) on network-level failures. ApiErrors (4xx/5xx) still throw immediately — they're deterministic. Removes the now-redundant `putWithRetry` from `ProjectPlanPage`. Web + workflow only; no iOS, server, or schema change.

---
