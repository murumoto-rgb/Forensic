## Build 6.25.1

* **Date:** 2026-06-10
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/review-code-visibility-izhPs
* **Merge SHA:** TBD
* **Summary:** **Reshoot-comparison discoverability (Wave 4).** The before/after comparison view (`PhotoComparisonView`-equivalent on iOS) has existed for a while but was reachable only through the photo row's ••• overflow menu — and only shown when the photo actually had lineage, so most users never learned the feature existed. The photo row now surfaces lineage directly: a tappable **teal "↻N" badge** in the row header (next to the group glyph) appears whenever the photo is the root of a reshoot chain (showing the reshoot count) or is itself a reshoot ("↻ reshoot"); tapping it opens the comparison view in one step. The overflow-menu entry stays. The badge reuses the row's existing `badge(text:color:)` capsule and carries an accessibility label. Per-row cost: the lineage check (`hasReshootLineage`) already ran for the menu; the count filter only executes for rows that have lineage. iOS-only; one file (`PhotoRow.swift`, conveniently its own file since #6.23.1). Backlog row updated. No DB / shared / server / web change.

---
