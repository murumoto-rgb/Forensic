## Build 5.83.1

* **Date:** 2026-06-08
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/trash-info
* **Merge SHA:** TBD
* **Summary:** Path P #8/8 (closing) — trash management on the Photos tab + editable project address on the Info tab + parity-matrix sign-off. New `web/src/components/TrashSection.tsx` adds a collapsible "🗑 Trash · N photos" section below the photo list (auto-hides when `trashedPhotos` is empty). Expanded view shows one row per trashed photo (seq #, caption preview, "Trashed at" timestamp), with Restore (clears `trashedAt`, moves the photo back from `trashedPhotos[]` to `photos[]`; preserves `sequenceNumber` to match iOS), Delete permanently (drops the photo from `trashedPhotos[]`), and a bulk "Empty trash" action — both destructive flows behind a confirm dialog that quotes the impact. The trash list intentionally skips the thumbnail batch fetch because restoring is usually a "seq # I deleted 5 minutes ago" workflow rather than a visual one, and the skip keeps the section cheap on projects with hundreds of trashed photos. `InfoTab.tsx` gains an editable address field — plain text input that writes `projectAddress` on blur; forward-geocoding (resolving address to GPS) remains iOS-only and was added to "Platform exclusions" with a one-line reason. `parity-matrix.md` closes Path P with a new section documenting each of the eight PRs (#110-#117, Builds #5.76.1 → #5.83.1) and lists explicit follow-ons that didn't make this series (full 3-level tag-selection picker UI, selection-scoped re-tag-with-AI, web file-upload, web canvas markup, report-branding, drag-and-drop bucket reorder). Closes the iOS↔web parity gap for everything except hardware-bound captures and PencilKit. No schema change, no shared API change, no server change, no iOS change. No `manifestSchemaVersion` bump.

---
