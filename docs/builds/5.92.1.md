## Build 5.92.1

* **Date:** 2026-06-08
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/web-report-branding
* **Merge SHA:** TBD
* **Summary:** Report branding editor on web — closes the last user-visible Settings parity gap. New shared `ReportBranding` interface in `packages/shared/src/appConfig.ts` (titleOverride, subtitleOverride, footerOverride, logoStoragePath — all nullable strings). New `ReportBrandingSchema` zod in validation.ts, plumbed into `AppConfigValueSchemaByKey` under the new `reportBranding` key. Server's existing app_config routes pick the new key up automatically via the schema map — no server code change. Parity contract test scopes `ReportBranding` into `APP_CONFIG_STRUCTS` (same exclusion as TagLibrary / AIRulesTemplate); 50/50 tests still pass. New admin page at `/admin/report-branding`: title/subtitle/footer text fields (commit on Save, optimistic-concurrency revision token, 409 banner), plus logo upload via the file-upload pipeline (`kind=markup_png` to scope-by-project — the upload endpoint requires a project id, so branding piggy-backs on the calling user's first project). Logo storage path is stashed in `logoStoragePath` for the PDF exporter to resolve to a presigned-GET at render time. Link added to Settings page → Team-wide config section. Web build #5.92.1. iOS-side struct + read-at-render integration is a follow-on iOS-only PR — the field is wire-stable today. No iOS change in this PR. No `manifestSchemaVersion` bump (this lives in app_config, not the project manifest).

---
