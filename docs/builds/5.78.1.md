## Build 5.78.1

* **Date:** 2026-06-08
* **PR:** [#112](https://github.com/murumoto-rgb/Forensic/pull/112)
* **Branch:** claude/photo-filters
* **Merge SHA:** 4bafe68
* **Summary:** Path P #3/8 — photo filter bar + search. The Photos tab gains a chip-style filter bar above the list with every per-photo axis iOS exposes: plan (all / no plan / specific), placed-vs-not-placed, date window (any / today / last 7 / last 30), bucket (multi-select OR, with a "No bucket" sentinel), tag (multi-select AND, threshold-aware), favorites-only, needs-review (any of `parseFailed` / `validationErrors.length > 0` / non-empty `reviewerFlag`), has-measurement, recommended-use (multi-select OR, sourced from distinct `aiAnalysis.recommendedUse` values in the project), plus a search input that substring-matches against sequence #, user/AI caption, user/AI observation, AI location-inferred, AI summary, and every tag label. Combination semantics mirror iOS: OR within an axis (bucket, use), AND within an axis (tag), AND across axes. New files: `web/src/lib/usePhotoFilters.ts` (state + applyFilters + isFilterActive + `usePhotoFilters` hook) and `web/src/components/PhotoFilterBar.tsx` (chip UI with `SelectChip` / `ToggleChip` / `MultiChip` sub-components). All client-side over the already-loaded manifest — no new API, no server change. `PhotosTab.tsx` reuses the existing `useTagConfidenceThreshold` hook so the tag-AND axis stays threshold-aware without a re-fetch. Filter bar header reads "Photos · N · M shown" matching iOS exactly. Clear-all chip resets state when any axis is active. Lightbox opens against the FILTERED set so prev/next nav stays inside the user's current scope. No schema change, no shared API change, no server change, no iOS change. No `manifestSchemaVersion` bump.

---
