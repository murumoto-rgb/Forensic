## Build 5.64.1

* **Date:** 2026-06-06
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/pdf-export-options
* **Merge SHA:** TBD
* **Summary:** Final piece of the server-side PDF export — PR #3 of 3 for plan item #3. Adds the options sheet so the office team picks page size, photos-per-page, photo filter, and what sections to include rather than always producing the fixed #5.63.1 layout. New migration `0008_pdf_export_options.sql` adds a JSONB `options` column to `pdf_export_jobs` (schema enforced in the application layer — keeps the column evolvable without another migration when a future PR adds a new field). New shared types in `packages/shared/src/api.ts`: `PdfPageSize` ('letter'|'a4'), `PdfPhotosPerPage` (1|2|4), `PdfPhotoFilter` ('all'|'favorites'|'byFloorPlan'), `PdfExportOptions` interface with seven fields, `CreatePdfExportRequest.options` is now `Partial<PdfExportOptions>` (still optional — empty body works for legacy clients). New server-side defaults helper `server/src/exports/options.ts` exporting `DEFAULT_PDF_EXPORT_OPTIONS` + `applyOptionDefaults(partial)`; defaults match the prior #5.63.1 output (all photos, 1 per page, Letter, cover + plans on). Server route updates in `routes/exports.ts`: zod `PdfExportOptionsSchema` + `CreateBodySchema` validate the request; 400 when `photoFilter === "byFloorPlan"` without a `floorPlanId`; options stored on the row. Worker reads options from the claimed row and passes to renderer. `pdfWorker.ts` chooses `Letter` vs `A4` for Puppeteer's `page.pdf({format})`. `htmlReport.ts` extended: new `selectPhotos(project, options)` applies the filter + trashed handling, new `chunk(arr, size)` groups photos for 2-up / 4-up layouts, photo pages now optionally wrap in `<section class="photogrid photogrid-2|4">` with CSS grid + scaled-down image heights + collapsed AI fields/observation/secondary tags so multi-up tiles fit on one printed page. Plan pages still always show every plan with ALL its pins (engineers want the spatial picture even on a subset export). Cover page now conditional on `options.includeCoverPage`; plan pages conditional on `options.includeFloorPlanPages`. Cap message tightened — checks selected count, not project count: "Selection contains N photos; cap is 200. Narrow the filter or trash unused." New `ExportOptionsModal` on `web/src/components/ExportPdfControl.tsx` shown when the user clicks Export PDF; lets them pick all of the above with sensible defaults. The "By floor plan" option only appears when the project has plans; submit disabled until a plan is picked. `web/src/lib/api.ts` `createPdfExport(projectId, options?)` now takes the options. `ProjectDetailPage` passes `floorPlans: {id, label}[]` to the control. No iOS change. Server + shared + web. **Operator step before merge:** run `0008_pdf_export_options.sql` in Supabase SQL editor. No `manifestSchemaVersion` bump.

---
