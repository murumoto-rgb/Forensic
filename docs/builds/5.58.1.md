## Build 5.58.1

* **Date:** 2026-06-06
* **PR:** [#88](https://github.com/murumoto-rgb/Forensic/pull/88)
* **Branch:** claude/docs-backup-drill-ops-runbook
* **Merge SHA:** 40d2184
* **Summary:** Items **#7** and **#8** from the queue, bundled — docs-only. New `docs/ops-runbook.md` (day-2 operations): a "what's running where" table covering Supabase / Render / Vercel / R2 / iOS / Sentry / PostHog, the one-paragraph data-flow summary, health-check commands (`/healthz` gitShaShort matching, Render cold-start behaviour), where to read logs per surface, and a common-incident playbook (server down, photos won't load, AI tagging 503s, sync-files-check 500, stale web data, concurrent-edit loss, deploys + rollback, routine maintenance). New `docs/backup-restore-drill.md` (recovery procedure): the "what's stored where" three-tier table (iOS local → iCloud → backend), the three independent manifest-backup mechanisms (Supabase PITR, audit_log replay, pull-from-iPad), per-failure-class recovery procedures (A single overwritten field → F total R2 loss, including the cloud-only fresh-install recovery path that #5.49.1/#5.53.1 made possible), and a quarterly drill checklist with a dated log table to fill in. `docs/deploy.md` env-var tables extended to include the R2, Anthropic, and #5.57.1 Sentry / PostHog vars that were previously undocumented, plus a "Related docs" cross-link section. Docs-only PR — no iOS / web / server / shared / supabase code change.

---
