## Build 5.14.1

* **Date:** 2026-05-31
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/phase3-pin-drag-reposition
* **Merge SHA:** TBD
* **Summary:** Phase 3 PR-C-2 — drag photo pins to reposition on the web floor plan canvas. PhotoPin becomes draggable; on drag-end, the new `(planPixelX, planPixelY)` updates the local project state immediately (optimistic), then PUTs the mutated manifest to the server with the current `expectedRevision`. Save status pill ("Saving…", "Saved ✓", or "Save failed") sits in the toolbar. Rapid successive drags coalesce — only one PUT in flight at a time; the most-recent local state is saved when the current PUT returns. 409 from the server (someone else wrote in between) shows a red banner asking the user to reload. Web-only; no iOS, server, or schema change. iPhone sees the new pin position on next sync pull.

---
