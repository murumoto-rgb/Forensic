## Build 5.101.1

* **Date:** 2026-06-08
* **PR:** [#135](https://github.com/murumoto-rgb/Forensic/pull/135)
* **Branch:** claude/web-filter-chips
* **Merge SHA:** f4d5a93
* **Summary:** Round 3 PR #5 — web filter chip catchup. The audit found that the three other filter chips the plan called out — "Measurements only", "Not in any bucket", "Located / not-located" — were already on web (via `hasMeasurement`, the `UNBUCKETED` sentinel chip, and the `placedFilter` dropdown). The only missing chip was iOS's "Validation issues only" (stricter than "Needs review" — checks only `parseFailed` + `validationErrors.length > 0`, ignoring the looser `reviewerFlag` annotation). This PR adds it. `PhotoFilterState` gains `validationIssuesOnly: boolean`. New `validationIssueMatch` helper in `usePhotoFilters.ts` runs the stricter check. New ToggleChip "✗ Validation issues" sits between "Needs review" and "Measurement" in the filter bar with an explanatory tooltip via the new optional `title` prop on `ToggleChip`. No iOS change. No schema change. No `manifestSchemaVersion` bump.

---
