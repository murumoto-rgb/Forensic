## Build 6.33.1

* **Date:** 2026-06-11
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/p2a-export-gate
* **Merge SHA:** TBD
* **Summary:** **Web exports work on frozen / unlocked projects (closes a P2 reviewer finding).** Web disabled PDF/CSV/folder exports unless the user held the edit lock AND the project wasn't frozen — `canEdit = lock.status.kind === "holding" && manifest.hasWriteAccess && !isFrozen` (`web/src/pages/ProjectWorkspacePage.tsx:58`) was the single gate, propagated as `canExport={canEdit}` into every export control on `ExportTab.tsx:51, 71, 86`. iOS deliberately keeps Export available on finalized read-only projects (`ProjectDetailView.swift:776` — `exportSection` is outside the `&& !frozen` gate); finalizing a report should not prevent reading it out. iOS also doesn't gate export on the edit lock. **Fix:** split `canExport` out from `canEdit` in the workspace shell — `canExport = manifest.hasWriteAccess` (any account with write access on the project; viewers still can't export, matching iOS) and `canEdit = canExport && lock.status.kind === "holding" && !isFrozen` (editing keeps both extra conditions). `ExportTab`'s prop renamed from `canEdit` to `canExport` and threaded into the four call sites (PDF control + folder client + server-side folder fallback + CSV). **Behavior delta:** (1) freeze a project on web → PDF/CSV/folder buttons remain enabled, mirroring iOS; (2) a second tab without the edit lock can export concurrently; (3) viewer accounts (the "read-only viewer" tier from #5.125.1) still see disabled exports — that bit was correct and stays. **Parity check:** no manifest model touched, no schema/zod change. The "Export PDF" / "Export CSV" / "Folder by Bucket" matrix rows already show ✅ on both platforms — that was a documentation truth; this PR makes it a code truth (the row notes editing-when-frozen are about iOS, not export-when-frozen). The new `planCoords.ts` from #6.32.1 is unaffected. Web typecheck + production build verified in CI.
