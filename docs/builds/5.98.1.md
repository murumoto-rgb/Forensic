## Build 5.98.1

* **Date:** 2026-06-08
* **PR:** [#132](https://github.com/murumoto-rgb/Forensic/pull/132)
* **Branch:** claude/web-exports-page
* **Merge SHA:** 28065e2
* **Summary:** Round 3 PR #2 — web UI for the unified Exports surface that PR #1 landed on the server. New `web/src/pages/ProjectExportsPage.tsx` lives at `/projects/:id/exports`, lists every export (newest first), polls in-flight jobs every 3 seconds, surfaces Download (presigned-GET) and Delete buttons. Status pills (queued / running / done / failed) + size + age. The Export tab in the workspace shell (`ExportTab.tsx`) is reorganized into a three-card grid (PDF / Folder by Bucket / AI Analysis CSV) plus a "View all exports →" link to the new page. `FolderExportControl.tsx` exposes the Folder-by-Bucket modal with a "Burn timestamp + GPS" toggle (UI wired; server worker doesn't honor it yet — flagged in the modal). CSV export uses a minimal inline trigger today (full options modal lands in PR #3 of Round 3). New `api.createProjectExport`, `api.listProjectExports`, `api.getProjectExport`, `api.deleteProjectExport` helpers wrap the four new endpoints from PR #1. The `deleteProjectExport` helper uses raw `fetch` (like `hardDeleteProject`) because the 204 response has no JSON body. PDF exports still flow through the legacy `pdf_export_jobs` queue for now — adapting PDFs onto `project_exports` is a follow-on cleanup; the current PR #1 design lets both coexist. No iOS change. No schema change. No `manifestSchemaVersion` bump.

---
