## Build 5.39.1

* **Date:** 2026-06-04
* **PR:** [#XX](https://github.com/murumoto-rgb/Forensic/pull/XX) (to be assigned)
* **Branch:** claude/web-ai-analysis-viewer
* **Merge SHA:** TBD
* **Summary:** Sprint A2 of the AI-tagging completion plan — read-only AI analysis viewer in the floor-plan preview panel. Surfaces the structured fields the model emitted that the panel's caption + observation lines don't already cover: `recommendedUse`, `scalePresent`, `measurementVisible`, `locationInferred`, `reviewerFlag`, the tag tree with per-secondary confidence percentages, and any `validationErrors` iOS recorded on the previous tag run. New `AIAnalysisViewer` + `AnalysisRow` + `AITagTree` components inside `PhotoPreviewPanel.tsx`; renders only when `photo.aiAnalysis != null`, wrapped in a `<details open>` so the user can collapse it. Reviewer flag renders in amber. Tag tree groups primaries with their secondaries as chips; each chip carries the lowercase-keyed confidence lookup the iOS pipeline uses (so a chip labelled "Drainage visually away from foundation" finds its 78% confidence regardless of map-key casing). Empty / null / iOS-sentinel field values are skipped so a sparsely-populated analysis doesn't show a wall of dashes. Parse-failure case renders a yellow "parse failed" badge next to the "AI analysis" heading and reveals the raw response in a nested `<details>` for the engineer to inspect. Web-only PR, pure UI on top of data web has been receiving from #5.37.1. No iOS, server, shared schema, or `manifestSchemaVersion` change.

---
